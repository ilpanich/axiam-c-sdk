/*
 * Generated by scripts/gen_management.py from management-registry.json and openapi.json.
 * DO NOT EDIT -- your changes will be overwritten. Regenerate with
 * `python3 scripts/gen_management.py`; CI verifies the committed output is current.
 */


#include <stdlib.h>
#include <string.h>

#include "unity.h"
#include "axiam/axiam.h"
#include "axiam/management_models.h"
#include "cJSON.h"

/*
 * Round-trip coverage for every 27 model. Generated by scripts/gen_management.py; CI
 * re-runs it with --check.
 */

/*
 * The generated parse/build pair is internal -- a caller has the typed operations and never
 * hands a raw cJSON to a model -- so this test declares them itself rather than the header
 * exposing them for a test's benefit.
 */
axiam_mgmt_add_member_request_t *axiam_mgmt_add_member_request_parse(const cJSON *src);
cJSON *axiam_mgmt_add_member_request_build(const axiam_mgmt_add_member_request_t *value);
axiam_mgmt_add_service_account_member_request_t *axiam_mgmt_add_service_account_member_request_parse(const cJSON *src);
cJSON *axiam_mgmt_add_service_account_member_request_build(const axiam_mgmt_add_service_account_member_request_t *value);
axiam_mgmt_api_provider_config_t *axiam_mgmt_api_provider_config_parse(const cJSON *src);
cJSON *axiam_mgmt_api_provider_config_build(const axiam_mgmt_api_provider_config_t *value);
axiam_mgmt_assign_role_to_group_request_t *axiam_mgmt_assign_role_to_group_request_parse(const cJSON *src);
cJSON *axiam_mgmt_assign_role_to_group_request_build(const axiam_mgmt_assign_role_to_group_request_t *value);
axiam_mgmt_assign_role_to_service_account_request_t *axiam_mgmt_assign_role_to_service_account_request_parse(const cJSON *src);
cJSON *axiam_mgmt_assign_role_to_service_account_request_build(const axiam_mgmt_assign_role_to_service_account_request_t *value);
axiam_mgmt_assign_role_to_user_request_t *axiam_mgmt_assign_role_to_user_request_parse(const cJSON *src);
cJSON *axiam_mgmt_assign_role_to_user_request_build(const axiam_mgmt_assign_role_to_user_request_t *value);
axiam_mgmt_audit_log_entry_t *axiam_mgmt_audit_log_entry_parse(const cJSON *src);
cJSON *axiam_mgmt_audit_log_entry_build(const axiam_mgmt_audit_log_entry_t *value);
axiam_mgmt_bind_certificate_t *axiam_mgmt_bind_certificate_parse(const cJSON *src);
cJSON *axiam_mgmt_bind_certificate_build(const axiam_mgmt_bind_certificate_t *value);
axiam_mgmt_ca_certificate_t *axiam_mgmt_ca_certificate_parse(const cJSON *src);
cJSON *axiam_mgmt_ca_certificate_build(const axiam_mgmt_ca_certificate_t *value);
axiam_mgmt_certificate_t *axiam_mgmt_certificate_parse(const cJSON *src);
cJSON *axiam_mgmt_certificate_build(const axiam_mgmt_certificate_t *value);
axiam_mgmt_certificate_policy_t *axiam_mgmt_certificate_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_certificate_policy_build(const axiam_mgmt_certificate_policy_t *value);
axiam_mgmt_compliance_report_entry_t *axiam_mgmt_compliance_report_entry_parse(const cJSON *src);
cJSON *axiam_mgmt_compliance_report_entry_build(const axiam_mgmt_compliance_report_entry_t *value);
axiam_mgmt_create_ca_certificate_request_t *axiam_mgmt_create_ca_certificate_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_ca_certificate_request_build(const axiam_mgmt_create_ca_certificate_request_t *value);
axiam_mgmt_create_certificate_request_t *axiam_mgmt_create_certificate_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_certificate_request_build(const axiam_mgmt_create_certificate_request_t *value);
axiam_mgmt_create_federation_config_request_t *axiam_mgmt_create_federation_config_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_federation_config_request_build(const axiam_mgmt_create_federation_config_request_t *value);
axiam_mgmt_create_group_request_t *axiam_mgmt_create_group_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_group_request_build(const axiam_mgmt_create_group_request_t *value);
axiam_mgmt_create_intermediate_ca_request_t *axiam_mgmt_create_intermediate_ca_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_intermediate_ca_request_build(const axiam_mgmt_create_intermediate_ca_request_t *value);
axiam_mgmt_create_notification_rule_request_t *axiam_mgmt_create_notification_rule_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_notification_rule_request_build(const axiam_mgmt_create_notification_rule_request_t *value);
axiam_mgmt_create_o_auth2_client_request_t *axiam_mgmt_create_o_auth2_client_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_o_auth2_client_request_build(const axiam_mgmt_create_o_auth2_client_request_t *value);
axiam_mgmt_create_permission_request_t *axiam_mgmt_create_permission_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_permission_request_build(const axiam_mgmt_create_permission_request_t *value);
axiam_mgmt_create_pgp_key_request_t *axiam_mgmt_create_pgp_key_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_pgp_key_request_build(const axiam_mgmt_create_pgp_key_request_t *value);
axiam_mgmt_create_reactor_request_t *axiam_mgmt_create_reactor_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_reactor_request_build(const axiam_mgmt_create_reactor_request_t *value);
axiam_mgmt_create_resource_request_t *axiam_mgmt_create_resource_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_resource_request_build(const axiam_mgmt_create_resource_request_t *value);
axiam_mgmt_create_role_request_t *axiam_mgmt_create_role_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_role_request_build(const axiam_mgmt_create_role_request_t *value);
axiam_mgmt_create_scim_token_request_t *axiam_mgmt_create_scim_token_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_scim_token_request_build(const axiam_mgmt_create_scim_token_request_t *value);
axiam_mgmt_create_scim_token_response_t *axiam_mgmt_create_scim_token_response_parse(const cJSON *src);
cJSON *axiam_mgmt_create_scim_token_response_build(const axiam_mgmt_create_scim_token_response_t *value);
axiam_mgmt_create_scope_request_t *axiam_mgmt_create_scope_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_scope_request_build(const axiam_mgmt_create_scope_request_t *value);
axiam_mgmt_create_service_account_request_t *axiam_mgmt_create_service_account_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_service_account_request_build(const axiam_mgmt_create_service_account_request_t *value);
axiam_mgmt_create_tenant_request_t *axiam_mgmt_create_tenant_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_tenant_request_build(const axiam_mgmt_create_tenant_request_t *value);
axiam_mgmt_create_user_request_t *axiam_mgmt_create_user_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_user_request_build(const axiam_mgmt_create_user_request_t *value);
axiam_mgmt_create_webhook_request_t *axiam_mgmt_create_webhook_request_parse(const cJSON *src);
cJSON *axiam_mgmt_create_webhook_request_build(const axiam_mgmt_create_webhook_request_t *value);
axiam_mgmt_email_config_t *axiam_mgmt_email_config_parse(const cJSON *src);
cJSON *axiam_mgmt_email_config_build(const axiam_mgmt_email_config_t *value);
axiam_mgmt_email_config_override_t *axiam_mgmt_email_config_override_parse(const cJSON *src);
cJSON *axiam_mgmt_email_config_override_build(const axiam_mgmt_email_config_override_t *value);
axiam_mgmt_email_test_result_t *axiam_mgmt_email_test_result_parse(const cJSON *src);
cJSON *axiam_mgmt_email_test_result_build(const axiam_mgmt_email_test_result_t *value);
axiam_mgmt_email_verification_policy_t *axiam_mgmt_email_verification_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_email_verification_policy_build(const axiam_mgmt_email_verification_policy_t *value);
axiam_mgmt_encrypt_request_t *axiam_mgmt_encrypt_request_parse(const cJSON *src);
cJSON *axiam_mgmt_encrypt_request_build(const axiam_mgmt_encrypt_request_t *value);
axiam_mgmt_encrypted_export_t *axiam_mgmt_encrypted_export_parse(const cJSON *src);
cJSON *axiam_mgmt_encrypted_export_build(const axiam_mgmt_encrypted_export_t *value);
axiam_mgmt_federation_config_response_t *axiam_mgmt_federation_config_response_parse(const cJSON *src);
cJSON *axiam_mgmt_federation_config_response_build(const axiam_mgmt_federation_config_response_t *value);
axiam_mgmt_federation_link_response_t *axiam_mgmt_federation_link_response_parse(const cJSON *src);
cJSON *axiam_mgmt_federation_link_response_build(const axiam_mgmt_federation_link_response_t *value);
axiam_mgmt_generated_ca_certificate_t *axiam_mgmt_generated_ca_certificate_parse(const cJSON *src);
cJSON *axiam_mgmt_generated_ca_certificate_build(const axiam_mgmt_generated_ca_certificate_t *value);
axiam_mgmt_generated_certificate_t *axiam_mgmt_generated_certificate_parse(const cJSON *src);
cJSON *axiam_mgmt_generated_certificate_build(const axiam_mgmt_generated_certificate_t *value);
axiam_mgmt_generated_pgp_key_t *axiam_mgmt_generated_pgp_key_parse(const cJSON *src);
cJSON *axiam_mgmt_generated_pgp_key_build(const axiam_mgmt_generated_pgp_key_t *value);
axiam_mgmt_grant_permission_request_t *axiam_mgmt_grant_permission_request_parse(const cJSON *src);
cJSON *axiam_mgmt_grant_permission_request_build(const axiam_mgmt_grant_permission_request_t *value);
axiam_mgmt_granted_scope_t *axiam_mgmt_granted_scope_parse(const cJSON *src);
cJSON *axiam_mgmt_granted_scope_build(const axiam_mgmt_granted_scope_t *value);
axiam_mgmt_group_t *axiam_mgmt_group_parse(const cJSON *src);
cJSON *axiam_mgmt_group_build(const axiam_mgmt_group_t *value);
axiam_mgmt_health_response_t *axiam_mgmt_health_response_parse(const cJSON *src);
cJSON *axiam_mgmt_health_response_build(const axiam_mgmt_health_response_t *value);
axiam_mgmt_import_ca_certificate_request_t *axiam_mgmt_import_ca_certificate_request_parse(const cJSON *src);
cJSON *axiam_mgmt_import_ca_certificate_request_build(const axiam_mgmt_import_ca_certificate_request_t *value);
axiam_mgmt_lockout_policy_t *axiam_mgmt_lockout_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_lockout_policy_build(const axiam_mgmt_lockout_policy_t *value);
axiam_mgmt_mds_refresh_outcome_t *axiam_mgmt_mds_refresh_outcome_parse(const cJSON *src);
cJSON *axiam_mgmt_mds_refresh_outcome_build(const axiam_mgmt_mds_refresh_outcome_t *value);
axiam_mgmt_mds_status_response_t *axiam_mgmt_mds_status_response_parse(const cJSON *src);
cJSON *axiam_mgmt_mds_status_response_build(const axiam_mgmt_mds_status_response_t *value);
axiam_mgmt_mfa_method_response_t *axiam_mgmt_mfa_method_response_parse(const cJSON *src);
cJSON *axiam_mgmt_mfa_method_response_build(const axiam_mgmt_mfa_method_response_t *value);
axiam_mgmt_mfa_policy_t *axiam_mgmt_mfa_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_mfa_policy_build(const axiam_mgmt_mfa_policy_t *value);
axiam_mgmt_migrate_custody_response_t *axiam_mgmt_migrate_custody_response_parse(const cJSON *src);
cJSON *axiam_mgmt_migrate_custody_response_build(const axiam_mgmt_migrate_custody_response_t *value);
axiam_mgmt_mtls_trust_anchor_response_t *axiam_mgmt_mtls_trust_anchor_response_parse(const cJSON *src);
cJSON *axiam_mgmt_mtls_trust_anchor_response_build(const axiam_mgmt_mtls_trust_anchor_response_t *value);
axiam_mgmt_notification_policy_t *axiam_mgmt_notification_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_notification_policy_build(const axiam_mgmt_notification_policy_t *value);
axiam_mgmt_notification_rule_response_t *axiam_mgmt_notification_rule_response_parse(const cJSON *src);
cJSON *axiam_mgmt_notification_rule_response_build(const axiam_mgmt_notification_rule_response_t *value);
axiam_mgmt_o_auth2_client_created_response_t *axiam_mgmt_o_auth2_client_created_response_parse(const cJSON *src);
cJSON *axiam_mgmt_o_auth2_client_created_response_build(const axiam_mgmt_o_auth2_client_created_response_t *value);
axiam_mgmt_o_auth2_client_response_t *axiam_mgmt_o_auth2_client_response_parse(const cJSON *src);
cJSON *axiam_mgmt_o_auth2_client_response_build(const axiam_mgmt_o_auth2_client_response_t *value);
axiam_mgmt_oidc_authorize_request_t *axiam_mgmt_oidc_authorize_request_parse(const cJSON *src);
cJSON *axiam_mgmt_oidc_authorize_request_build(const axiam_mgmt_oidc_authorize_request_t *value);
axiam_mgmt_oidc_authorize_response_t *axiam_mgmt_oidc_authorize_response_parse(const cJSON *src);
cJSON *axiam_mgmt_oidc_authorize_response_build(const axiam_mgmt_oidc_authorize_response_t *value);
axiam_mgmt_oidc_callback_request_t *axiam_mgmt_oidc_callback_request_parse(const cJSON *src);
cJSON *axiam_mgmt_oidc_callback_request_build(const axiam_mgmt_oidc_callback_request_t *value);
axiam_mgmt_oidc_callback_response_t *axiam_mgmt_oidc_callback_response_parse(const cJSON *src);
cJSON *axiam_mgmt_oidc_callback_response_build(const axiam_mgmt_oidc_callback_response_t *value);
axiam_mgmt_opaque_enrollment_t *axiam_mgmt_opaque_enrollment_parse(const cJSON *src);
cJSON *axiam_mgmt_opaque_enrollment_build(const axiam_mgmt_opaque_enrollment_t *value);
axiam_mgmt_opaque_policy_t *axiam_mgmt_opaque_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_opaque_policy_build(const axiam_mgmt_opaque_policy_t *value);
axiam_mgmt_organization_t *axiam_mgmt_organization_parse(const cJSON *src);
cJSON *axiam_mgmt_organization_build(const axiam_mgmt_organization_t *value);
axiam_mgmt_password_policy_t *axiam_mgmt_password_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_password_policy_build(const axiam_mgmt_password_policy_t *value);
axiam_mgmt_permission_t *axiam_mgmt_permission_parse(const cJSON *src);
cJSON *axiam_mgmt_permission_build(const axiam_mgmt_permission_t *value);
axiam_mgmt_pgp_key_t *axiam_mgmt_pgp_key_parse(const cJSON *src);
cJSON *axiam_mgmt_pgp_key_build(const axiam_mgmt_pgp_key_t *value);
axiam_mgmt_policy_response_t *axiam_mgmt_policy_response_parse(const cJSON *src);
cJSON *axiam_mgmt_policy_response_build(const axiam_mgmt_policy_response_t *value);
axiam_mgmt_privacy_policy_t *axiam_mgmt_privacy_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_privacy_policy_build(const axiam_mgmt_privacy_policy_t *value);
axiam_mgmt_provider_config_t *axiam_mgmt_provider_config_parse(const cJSON *src);
cJSON *axiam_mgmt_provider_config_build(const axiam_mgmt_provider_config_t *value);
axiam_mgmt_reactor_event_descriptor_t *axiam_mgmt_reactor_event_descriptor_parse(const cJSON *src);
cJSON *axiam_mgmt_reactor_event_descriptor_build(const axiam_mgmt_reactor_event_descriptor_t *value);
axiam_mgmt_reactor_response_t *axiam_mgmt_reactor_response_parse(const cJSON *src);
cJSON *axiam_mgmt_reactor_response_build(const axiam_mgmt_reactor_response_t *value);
axiam_mgmt_ready_response_t *axiam_mgmt_ready_response_parse(const cJSON *src);
cJSON *axiam_mgmt_ready_response_build(const axiam_mgmt_ready_response_t *value);
axiam_mgmt_resolved_permission_grant_t *axiam_mgmt_resolved_permission_grant_parse(const cJSON *src);
cJSON *axiam_mgmt_resolved_permission_grant_build(const axiam_mgmt_resolved_permission_grant_t *value);
axiam_mgmt_resource_t *axiam_mgmt_resource_parse(const cJSON *src);
cJSON *axiam_mgmt_resource_build(const axiam_mgmt_resource_t *value);
axiam_mgmt_retry_policy_t *axiam_mgmt_retry_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_retry_policy_build(const axiam_mgmt_retry_policy_t *value);
axiam_mgmt_role_t *axiam_mgmt_role_parse(const cJSON *src);
cJSON *axiam_mgmt_role_build(const axiam_mgmt_role_t *value);
axiam_mgmt_role_assignment_t *axiam_mgmt_role_assignment_parse(const cJSON *src);
cJSON *axiam_mgmt_role_assignment_build(const axiam_mgmt_role_assignment_t *value);
axiam_mgmt_role_group_assignment_t *axiam_mgmt_role_group_assignment_parse(const cJSON *src);
cJSON *axiam_mgmt_role_group_assignment_build(const axiam_mgmt_role_group_assignment_t *value);
axiam_mgmt_role_service_account_assignment_t *axiam_mgmt_role_service_account_assignment_parse(const cJSON *src);
cJSON *axiam_mgmt_role_service_account_assignment_build(const axiam_mgmt_role_service_account_assignment_t *value);
axiam_mgmt_role_user_assignment_t *axiam_mgmt_role_user_assignment_parse(const cJSON *src);
cJSON *axiam_mgmt_role_user_assignment_build(const axiam_mgmt_role_user_assignment_t *value);
axiam_mgmt_rotate_secret_response_t *axiam_mgmt_rotate_secret_response_parse(const cJSON *src);
cJSON *axiam_mgmt_rotate_secret_response_build(const axiam_mgmt_rotate_secret_response_t *value);
axiam_mgmt_scim_token_response_t *axiam_mgmt_scim_token_response_parse(const cJSON *src);
cJSON *axiam_mgmt_scim_token_response_build(const axiam_mgmt_scim_token_response_t *value);
axiam_mgmt_scope_t *axiam_mgmt_scope_parse(const cJSON *src);
cJSON *axiam_mgmt_scope_build(const axiam_mgmt_scope_t *value);
axiam_mgmt_security_settings_t *axiam_mgmt_security_settings_parse(const cJSON *src);
cJSON *axiam_mgmt_security_settings_build(const axiam_mgmt_security_settings_t *value);
axiam_mgmt_service_account_created_response_t *axiam_mgmt_service_account_created_response_parse(const cJSON *src);
cJSON *axiam_mgmt_service_account_created_response_build(const axiam_mgmt_service_account_created_response_t *value);
axiam_mgmt_service_account_response_t *axiam_mgmt_service_account_response_parse(const cJSON *src);
cJSON *axiam_mgmt_service_account_response_build(const axiam_mgmt_service_account_response_t *value);
axiam_mgmt_set_mtls_trust_anchor_t *axiam_mgmt_set_mtls_trust_anchor_parse(const cJSON *src);
cJSON *axiam_mgmt_set_mtls_trust_anchor_build(const axiam_mgmt_set_mtls_trust_anchor_t *value);
axiam_mgmt_set_org_email_config_t *axiam_mgmt_set_org_email_config_parse(const cJSON *src);
cJSON *axiam_mgmt_set_org_email_config_build(const axiam_mgmt_set_org_email_config_t *value);
axiam_mgmt_set_org_settings_t *axiam_mgmt_set_org_settings_parse(const cJSON *src);
cJSON *axiam_mgmt_set_org_settings_build(const axiam_mgmt_set_org_settings_t *value);
axiam_mgmt_sign_audit_batch_request_t *axiam_mgmt_sign_audit_batch_request_parse(const cJSON *src);
cJSON *axiam_mgmt_sign_audit_batch_request_build(const axiam_mgmt_sign_audit_batch_request_t *value);
axiam_mgmt_sign_intermediate_csr_request_t *axiam_mgmt_sign_intermediate_csr_request_parse(const cJSON *src);
cJSON *axiam_mgmt_sign_intermediate_csr_request_build(const axiam_mgmt_sign_intermediate_csr_request_t *value);
axiam_mgmt_signed_audit_batch_t *axiam_mgmt_signed_audit_batch_parse(const cJSON *src);
cJSON *axiam_mgmt_signed_audit_batch_build(const axiam_mgmt_signed_audit_batch_t *value);
axiam_mgmt_smtp_config_t *axiam_mgmt_smtp_config_parse(const cJSON *src);
cJSON *axiam_mgmt_smtp_config_build(const axiam_mgmt_smtp_config_t *value);
axiam_mgmt_tenant_t *axiam_mgmt_tenant_parse(const cJSON *src);
cJSON *axiam_mgmt_tenant_build(const axiam_mgmt_tenant_t *value);
axiam_mgmt_tenant_settings_override_t *axiam_mgmt_tenant_settings_override_parse(const cJSON *src);
cJSON *axiam_mgmt_tenant_settings_override_build(const axiam_mgmt_tenant_settings_override_t *value);
axiam_mgmt_token_exchange_trust_request_t *axiam_mgmt_token_exchange_trust_request_parse(const cJSON *src);
cJSON *axiam_mgmt_token_exchange_trust_request_build(const axiam_mgmt_token_exchange_trust_request_t *value);
axiam_mgmt_token_exchange_trust_response_t *axiam_mgmt_token_exchange_trust_response_parse(const cJSON *src);
cJSON *axiam_mgmt_token_exchange_trust_response_build(const axiam_mgmt_token_exchange_trust_response_t *value);
axiam_mgmt_token_policy_t *axiam_mgmt_token_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_token_policy_build(const axiam_mgmt_token_policy_t *value);
axiam_mgmt_update_federation_config_request_t *axiam_mgmt_update_federation_config_request_parse(const cJSON *src);
cJSON *axiam_mgmt_update_federation_config_request_build(const axiam_mgmt_update_federation_config_request_t *value);
axiam_mgmt_update_group_t *axiam_mgmt_update_group_parse(const cJSON *src);
cJSON *axiam_mgmt_update_group_build(const axiam_mgmt_update_group_t *value);
axiam_mgmt_update_notification_rule_request_t *axiam_mgmt_update_notification_rule_request_parse(const cJSON *src);
cJSON *axiam_mgmt_update_notification_rule_request_build(const axiam_mgmt_update_notification_rule_request_t *value);
axiam_mgmt_update_o_auth2_client_request_t *axiam_mgmt_update_o_auth2_client_request_parse(const cJSON *src);
cJSON *axiam_mgmt_update_o_auth2_client_request_build(const axiam_mgmt_update_o_auth2_client_request_t *value);
axiam_mgmt_update_organization_request_t *axiam_mgmt_update_organization_request_parse(const cJSON *src);
cJSON *axiam_mgmt_update_organization_request_build(const axiam_mgmt_update_organization_request_t *value);
axiam_mgmt_update_permission_request_t *axiam_mgmt_update_permission_request_parse(const cJSON *src);
cJSON *axiam_mgmt_update_permission_request_build(const axiam_mgmt_update_permission_request_t *value);
axiam_mgmt_update_reactor_request_t *axiam_mgmt_update_reactor_request_parse(const cJSON *src);
cJSON *axiam_mgmt_update_reactor_request_build(const axiam_mgmt_update_reactor_request_t *value);
axiam_mgmt_update_resource_request_t *axiam_mgmt_update_resource_request_parse(const cJSON *src);
cJSON *axiam_mgmt_update_resource_request_build(const axiam_mgmt_update_resource_request_t *value);
axiam_mgmt_update_role_t *axiam_mgmt_update_role_parse(const cJSON *src);
cJSON *axiam_mgmt_update_role_build(const axiam_mgmt_update_role_t *value);
axiam_mgmt_update_scope_request_t *axiam_mgmt_update_scope_request_parse(const cJSON *src);
cJSON *axiam_mgmt_update_scope_request_build(const axiam_mgmt_update_scope_request_t *value);
axiam_mgmt_update_service_account_t *axiam_mgmt_update_service_account_parse(const cJSON *src);
cJSON *axiam_mgmt_update_service_account_build(const axiam_mgmt_update_service_account_t *value);
axiam_mgmt_update_tenant_t *axiam_mgmt_update_tenant_parse(const cJSON *src);
cJSON *axiam_mgmt_update_tenant_build(const axiam_mgmt_update_tenant_t *value);
axiam_mgmt_update_user_request_t *axiam_mgmt_update_user_request_parse(const cJSON *src);
cJSON *axiam_mgmt_update_user_request_build(const axiam_mgmt_update_user_request_t *value);
axiam_mgmt_update_webhook_request_t *axiam_mgmt_update_webhook_request_parse(const cJSON *src);
cJSON *axiam_mgmt_update_webhook_request_build(const axiam_mgmt_update_webhook_request_t *value);
axiam_mgmt_user_response_t *axiam_mgmt_user_response_parse(const cJSON *src);
cJSON *axiam_mgmt_user_response_build(const axiam_mgmt_user_response_t *value);
axiam_mgmt_webauthn_attestation_policy_t *axiam_mgmt_webauthn_attestation_policy_parse(const cJSON *src);
cJSON *axiam_mgmt_webauthn_attestation_policy_build(const axiam_mgmt_webauthn_attestation_policy_t *value);
axiam_mgmt_webhook_response_t *axiam_mgmt_webhook_response_parse(const cJSON *src);
cJSON *axiam_mgmt_webhook_response_build(const axiam_mgmt_webhook_response_t *value);

void setUp(void) {}
void tearDown(void) {}

/* `AddMemberRequest`: a full wire object parses, builds back and frees. */
static void test_add_member_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_add_member_request_t *model = axiam_mgmt_add_member_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_add_member_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_add_member_request_free(model);
    cJSON_Delete(src);
}

/*
 * `AddMemberRequest`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_add_member_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_add_member_request_t *model = axiam_mgmt_add_member_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_add_member_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_add_member_request_free(model);
    cJSON_Delete(src);
}

static void test_add_member_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_add_member_request_parse(scalar));
    axiam_mgmt_add_member_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `AddServiceAccountMemberRequest`: a full wire object parses, builds back and frees. */
static void test_add_service_account_member_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"service_account_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_add_service_account_member_request_t *model = axiam_mgmt_add_service_account_member_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_add_service_account_member_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_add_service_account_member_request_free(model);
    cJSON_Delete(src);
}

/*
 * `AddServiceAccountMemberRequest`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_add_service_account_member_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_add_service_account_member_request_t *model = axiam_mgmt_add_service_account_member_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_add_service_account_member_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_add_service_account_member_request_free(model);
    cJSON_Delete(src);
}

static void test_add_service_account_member_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_add_service_account_member_request_parse(scalar));
    axiam_mgmt_add_service_account_member_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `ApiProviderConfig`: a full wire object parses, builds back and frees. */
static void test_api_provider_config_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"api_url\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_api_provider_config_t *model = axiam_mgmt_api_provider_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_api_provider_config_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_api_provider_config_free(model);
    cJSON_Delete(src);
}

/* `ApiProviderConfig`: the server omitting every OPTIONAL field is not an error. */
static void test_api_provider_config_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_api_provider_config_t *model = axiam_mgmt_api_provider_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_api_provider_config_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_api_provider_config_free(model);
    cJSON_Delete(src);
}

/*
 * `ApiProviderConfig`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_api_provider_config_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_api_provider_config_t *model = axiam_mgmt_api_provider_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_api_provider_config_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_api_provider_config_free(model);
    cJSON_Delete(src);
}

static void test_api_provider_config_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_api_provider_config_parse(scalar));
    axiam_mgmt_api_provider_config_free(NULL);
    cJSON_Delete(scalar);
}

/* `AssignRoleToGroupRequest`: a full wire object parses, builds back and frees. */
static void test_assign_role_to_group_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"group_id\": \"11111111-1111-4111-8111-111111111111\", \"resource_id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_scope\": [\"11111111-1111-4111-8111-111111111111\"]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_assign_role_to_group_request_t *model = axiam_mgmt_assign_role_to_group_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_assign_role_to_group_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_assign_role_to_group_request_free(model);
    cJSON_Delete(src);
}

/* `AssignRoleToGroupRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_assign_role_to_group_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"group_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_assign_role_to_group_request_t *model = axiam_mgmt_assign_role_to_group_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_assign_role_to_group_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_assign_role_to_group_request_free(model);
    cJSON_Delete(src);
}

/*
 * `AssignRoleToGroupRequest`: an EMPTY object parses without crashing. A server that omits
 * a field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_assign_role_to_group_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_assign_role_to_group_request_t *model = axiam_mgmt_assign_role_to_group_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_assign_role_to_group_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_assign_role_to_group_request_free(model);
    cJSON_Delete(src);
}

static void test_assign_role_to_group_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_assign_role_to_group_request_parse(scalar));
    axiam_mgmt_assign_role_to_group_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `AssignRoleToServiceAccountRequest`: a full wire object parses, builds back and frees. */
static void test_assign_role_to_service_account_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"resource_id\": \"11111111-1111-4111-8111-111111111111\", \"service_account_id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_scope\": [\"11111111-1111-4111-8111-111111111111\"]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_assign_role_to_service_account_request_t *model = axiam_mgmt_assign_role_to_service_account_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_assign_role_to_service_account_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_assign_role_to_service_account_request_free(model);
    cJSON_Delete(src);
}

/*
 * `AssignRoleToServiceAccountRequest`: the server omitting every OPTIONAL field is not an
 * error.
 */
static void test_assign_role_to_service_account_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"service_account_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_assign_role_to_service_account_request_t *model = axiam_mgmt_assign_role_to_service_account_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_assign_role_to_service_account_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_assign_role_to_service_account_request_free(model);
    cJSON_Delete(src);
}

/*
 * `AssignRoleToServiceAccountRequest`: an EMPTY object parses without crashing. A server
 * that omits a field openapi.json marks required is malformed, and this SDK's answer is a
 * model with that member unset rather than an abort -- the caller is in a position to
 * decide, and a parser that segfaults on a bad response is a worse failure than one that
 * hands back a null. This also reaches the ABSENT arm of every field guard, required ones
 * included, which no well-formed fixture can.
 */
static void test_assign_role_to_service_account_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_assign_role_to_service_account_request_t *model = axiam_mgmt_assign_role_to_service_account_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_assign_role_to_service_account_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_assign_role_to_service_account_request_free(model);
    cJSON_Delete(src);
}

static void test_assign_role_to_service_account_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_assign_role_to_service_account_request_parse(scalar));
    axiam_mgmt_assign_role_to_service_account_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `AssignRoleToUserRequest`: a full wire object parses, builds back and frees. */
static void test_assign_role_to_user_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"resource_id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_scope\": [\"11111111-1111-4111-8111-111111111111\"], \"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_assign_role_to_user_request_t *model = axiam_mgmt_assign_role_to_user_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_assign_role_to_user_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_assign_role_to_user_request_free(model);
    cJSON_Delete(src);
}

/* `AssignRoleToUserRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_assign_role_to_user_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_assign_role_to_user_request_t *model = axiam_mgmt_assign_role_to_user_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_assign_role_to_user_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_assign_role_to_user_request_free(model);
    cJSON_Delete(src);
}

/*
 * `AssignRoleToUserRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_assign_role_to_user_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_assign_role_to_user_request_t *model = axiam_mgmt_assign_role_to_user_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_assign_role_to_user_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_assign_role_to_user_request_free(model);
    cJSON_Delete(src);
}

static void test_assign_role_to_user_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_assign_role_to_user_request_parse(scalar));
    axiam_mgmt_assign_role_to_user_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `AuditLogEntry`: a full wire object parses, builds back and frees. */
static void test_audit_log_entry_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"action\": \"example\", \"actor_id\": \"11111111-1111-4111-8111-111111111111\", \"actor_type\": \"User\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"ip_address\": \"example\", \"metadata\": {}, \"outcome\": \"Success\", \"resource_id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"timestamp\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_audit_log_entry_t *model = axiam_mgmt_audit_log_entry_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_audit_log_entry_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_audit_log_entry_free(model);
    cJSON_Delete(src);
}

/* `AuditLogEntry`: the server omitting every OPTIONAL field is not an error. */
static void test_audit_log_entry_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"action\": \"example\", \"actor_id\": \"11111111-1111-4111-8111-111111111111\", \"actor_type\": \"User\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"metadata\": {}, \"outcome\": \"Success\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"timestamp\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_audit_log_entry_t *model = axiam_mgmt_audit_log_entry_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_audit_log_entry_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_audit_log_entry_free(model);
    cJSON_Delete(src);
}

/*
 * `AuditLogEntry`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_audit_log_entry_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_audit_log_entry_t *model = axiam_mgmt_audit_log_entry_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_audit_log_entry_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_audit_log_entry_free(model);
    cJSON_Delete(src);
}

static void test_audit_log_entry_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_audit_log_entry_parse(scalar));
    axiam_mgmt_audit_log_entry_free(NULL);
    cJSON_Delete(scalar);
}

/* `BindCertificate`: a full wire object parses, builds back and frees. */
static void test_bind_certificate_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"certificate_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_bind_certificate_t *model = axiam_mgmt_bind_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_bind_certificate_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_bind_certificate_free(model);
    cJSON_Delete(src);
}

/*
 * `BindCertificate`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_bind_certificate_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_bind_certificate_t *model = axiam_mgmt_bind_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_bind_certificate_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_bind_certificate_free(model);
    cJSON_Delete(src);
}

static void test_bind_certificate_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_bind_certificate_parse(scalar));
    axiam_mgmt_bind_certificate_free(NULL);
    cJSON_Delete(scalar);
}

/* `CaCertificate`: a full wire object parses, builds back and frees. */
static void test_ca_certificate_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"chain_pem\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"fingerprint\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"key_algorithm\": \"Rsa4096\", \"key_custody\": \"example\", \"key_locator\": \"example\", \"mtls_trust_anchor\": true, \"not_after\": \"2026-08-26T00:00:00Z\", \"not_before\": \"2026-08-26T00:00:00Z\", \"organization_id\": \"11111111-1111-4111-8111-111111111111\", \"parent_ca_id\": \"11111111-1111-4111-8111-111111111111\", \"public_cert_pem\": \"example\", \"status\": \"Active\", \"subject\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_ca_certificate_t *model = axiam_mgmt_ca_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_ca_certificate_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_ca_certificate_free(model);
    cJSON_Delete(src);
}

/* `CaCertificate`: the server omitting every OPTIONAL field is not an error. */
static void test_ca_certificate_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"fingerprint\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"key_algorithm\": \"Rsa4096\", \"not_after\": \"2026-08-26T00:00:00Z\", \"not_before\": \"2026-08-26T00:00:00Z\", \"organization_id\": \"11111111-1111-4111-8111-111111111111\", \"public_cert_pem\": \"example\", \"status\": \"Active\", \"subject\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_ca_certificate_t *model = axiam_mgmt_ca_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_ca_certificate_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_ca_certificate_free(model);
    cJSON_Delete(src);
}

/*
 * `CaCertificate`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_ca_certificate_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_ca_certificate_t *model = axiam_mgmt_ca_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_ca_certificate_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_ca_certificate_free(model);
    cJSON_Delete(src);
}

static void test_ca_certificate_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_ca_certificate_parse(scalar));
    axiam_mgmt_ca_certificate_free(NULL);
    cJSON_Delete(scalar);
}

/* `Certificate`: a full wire object parses, builds back and frees. */
static void test_certificate_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"cert_type\": \"User\", \"created_at\": \"2026-08-26T00:00:00Z\", \"fingerprint\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"issuer_ca_id\": \"11111111-1111-4111-8111-111111111111\", \"key_algorithm\": \"Rsa4096\", \"metadata\": {}, \"not_after\": \"2026-08-26T00:00:00Z\", \"not_before\": \"2026-08-26T00:00:00Z\", \"public_cert_pem\": \"example\", \"status\": \"Active\", \"subject\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_certificate_t *model = axiam_mgmt_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_certificate_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_certificate_free(model);
    cJSON_Delete(src);
}

/*
 * `Certificate`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_certificate_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_certificate_t *model = axiam_mgmt_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_certificate_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_certificate_free(model);
    cJSON_Delete(src);
}

static void test_certificate_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_certificate_parse(scalar));
    axiam_mgmt_certificate_free(NULL);
    cJSON_Delete(scalar);
}

/* `CertificatePolicy`: a full wire object parses, builds back and frees. */
static void test_certificate_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"default_cert_validity_days\": 1, \"max_cert_validity_days\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_certificate_policy_t *model = axiam_mgmt_certificate_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_certificate_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_certificate_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `CertificatePolicy`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_certificate_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_certificate_policy_t *model = axiam_mgmt_certificate_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_certificate_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_certificate_policy_free(model);
    cJSON_Delete(src);
}

static void test_certificate_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_certificate_policy_parse(scalar));
    axiam_mgmt_certificate_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `ComplianceReportEntry`: a full wire object parses, builds back and frees. */
static void test_compliance_report_entry_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"aaguid\": \"11111111-1111-4111-8111-111111111111\", \"authenticator_name\": \"example\", \"compliant\": true, \"credential_id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"reason\": \"example\", \"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_compliance_report_entry_t *model = axiam_mgmt_compliance_report_entry_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_compliance_report_entry_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_compliance_report_entry_free(model);
    cJSON_Delete(src);
}

/* `ComplianceReportEntry`: the server omitting every OPTIONAL field is not an error. */
static void test_compliance_report_entry_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"compliant\": true, \"credential_id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_compliance_report_entry_t *model = axiam_mgmt_compliance_report_entry_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_compliance_report_entry_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_compliance_report_entry_free(model);
    cJSON_Delete(src);
}

/*
 * `ComplianceReportEntry`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_compliance_report_entry_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_compliance_report_entry_t *model = axiam_mgmt_compliance_report_entry_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_compliance_report_entry_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_compliance_report_entry_free(model);
    cJSON_Delete(src);
}

static void test_compliance_report_entry_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_compliance_report_entry_parse(scalar));
    axiam_mgmt_compliance_report_entry_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateCaCertificateRequest`: a full wire object parses, builds back and frees. */
static void test_create_ca_certificate_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"intermediate_subject\": \"example\", \"intermediate_validity_days\": 1, \"issue_from_root\": true, \"key_algorithm\": \"Rsa4096\", \"subject\": \"example\", \"validity_days\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_ca_certificate_request_t *model = axiam_mgmt_create_ca_certificate_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_ca_certificate_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_ca_certificate_request_free(model);
    cJSON_Delete(src);
}

/* `CreateCaCertificateRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_ca_certificate_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"key_algorithm\": \"Rsa4096\", \"subject\": \"example\", \"validity_days\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_ca_certificate_request_t *model = axiam_mgmt_create_ca_certificate_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_ca_certificate_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_ca_certificate_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateCaCertificateRequest`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_ca_certificate_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_ca_certificate_request_t *model = axiam_mgmt_create_ca_certificate_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_ca_certificate_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_ca_certificate_request_free(model);
    cJSON_Delete(src);
}

static void test_create_ca_certificate_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_ca_certificate_request_parse(scalar));
    axiam_mgmt_create_ca_certificate_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateCertificateRequest`: a full wire object parses, builds back and frees. */
static void test_create_certificate_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"cert_type\": \"User\", \"issuer_ca_id\": \"11111111-1111-4111-8111-111111111111\", \"key_algorithm\": \"Rsa4096\", \"metadata\": {}, \"subject\": \"example\", \"validity_days\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_certificate_request_t *model = axiam_mgmt_create_certificate_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_certificate_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_certificate_request_free(model);
    cJSON_Delete(src);
}

/* `CreateCertificateRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_certificate_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"cert_type\": \"User\", \"issuer_ca_id\": \"11111111-1111-4111-8111-111111111111\", \"key_algorithm\": \"Rsa4096\", \"subject\": \"example\", \"validity_days\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_certificate_request_t *model = axiam_mgmt_create_certificate_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_certificate_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_certificate_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateCertificateRequest`: an EMPTY object parses without crashing. A server that omits
 * a field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_certificate_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_certificate_request_t *model = axiam_mgmt_create_certificate_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_certificate_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_certificate_request_free(model);
    cJSON_Delete(src);
}

static void test_create_certificate_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_certificate_request_parse(scalar));
    axiam_mgmt_create_certificate_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateFederationConfigRequest`: a full wire object parses, builds back and frees. */
static void test_create_federation_config_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"allowed_algorithms\": [\"example\"], \"attribute_map\": {}, \"client_id\": \"example\", \"client_secret\": \"example\", \"idp_signing_cert_pem\": \"example\", \"metadata_url\": \"example\", \"protocol\": \"example\", \"provider\": \"example\", \"token_exchange\": {\"accepted_audiences\": [\"example\"], \"enabled\": true, \"max_lifetime_secs\": 1, \"max_token_age_secs\": 1, \"scope_map\": {}, \"subject_mapping\": \"example\"}}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_federation_config_request_t *model = axiam_mgmt_create_federation_config_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_federation_config_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_federation_config_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateFederationConfigRequest`: the server omitting every OPTIONAL field is not an
 * error.
 */
static void test_create_federation_config_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"client_id\": \"example\", \"client_secret\": \"example\", \"protocol\": \"example\", \"provider\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_federation_config_request_t *model = axiam_mgmt_create_federation_config_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_federation_config_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_federation_config_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateFederationConfigRequest`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_federation_config_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_federation_config_request_t *model = axiam_mgmt_create_federation_config_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_federation_config_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_federation_config_request_free(model);
    cJSON_Delete(src);
}

static void test_create_federation_config_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_federation_config_request_parse(scalar));
    axiam_mgmt_create_federation_config_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateGroupRequest`: a full wire object parses, builds back and frees. */
static void test_create_group_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"metadata\": {}, \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_group_request_t *model = axiam_mgmt_create_group_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_group_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_group_request_free(model);
    cJSON_Delete(src);
}

/* `CreateGroupRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_group_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_group_request_t *model = axiam_mgmt_create_group_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_group_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_group_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateGroupRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_group_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_group_request_t *model = axiam_mgmt_create_group_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_group_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_group_request_free(model);
    cJSON_Delete(src);
}

static void test_create_group_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_group_request_parse(scalar));
    axiam_mgmt_create_group_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateIntermediateCaRequest`: a full wire object parses, builds back and frees. */
static void test_create_intermediate_ca_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"key_algorithm\": \"Rsa4096\", \"parent_ca_id\": \"11111111-1111-4111-8111-111111111111\", \"subject\": \"example\", \"validity_days\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_intermediate_ca_request_t *model = axiam_mgmt_create_intermediate_ca_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_intermediate_ca_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_intermediate_ca_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateIntermediateCaRequest`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_intermediate_ca_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_intermediate_ca_request_t *model = axiam_mgmt_create_intermediate_ca_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_intermediate_ca_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_intermediate_ca_request_free(model);
    cJSON_Delete(src);
}

static void test_create_intermediate_ca_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_intermediate_ca_request_parse(scalar));
    axiam_mgmt_create_intermediate_ca_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateNotificationRuleRequest`: a full wire object parses, builds back and frees. */
static void test_create_notification_rule_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"events\": [\"login_failure\"], \"name\": \"example\", \"recipient_emails\": [\"example\"]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_notification_rule_request_t *model = axiam_mgmt_create_notification_rule_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_notification_rule_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_notification_rule_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateNotificationRuleRequest`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_notification_rule_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_notification_rule_request_t *model = axiam_mgmt_create_notification_rule_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_notification_rule_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_notification_rule_request_free(model);
    cJSON_Delete(src);
}

static void test_create_notification_rule_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_notification_rule_request_parse(scalar));
    axiam_mgmt_create_notification_rule_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateOAuth2ClientRequest`: a full wire object parses, builds back and frees. */
static void test_create_o_auth2_client_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"backchannel_logout_uri\": \"example\", \"dpop_bound_access_tokens\": true, \"dpop_require_nonce\": true, \"grant_types\": [\"example\"], \"jwks\": \"example\", \"jwks_uri\": \"example\", \"name\": \"example\", \"post_logout_redirect_uris\": [\"example\"], \"profile\": \"standard\", \"redirect_uris\": [\"example\"], \"require_par\": true, \"scopes\": [\"example\"], \"self_signed_tls_client_auth_thumbprints\": [\"example\"], \"tls_client_auth_san_dns\": \"example\", \"tls_client_auth_san_uri\": \"example\", \"tls_client_auth_subject_dn\": \"example\", \"tls_client_certificate_bound_access_tokens\": true, \"token_endpoint_auth_method\": \"client_secret_post\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_o_auth2_client_request_t *model = axiam_mgmt_create_o_auth2_client_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_o_auth2_client_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_o_auth2_client_request_free(model);
    cJSON_Delete(src);
}

/* `CreateOAuth2ClientRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_o_auth2_client_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"grant_types\": [\"example\"], \"name\": \"example\", \"redirect_uris\": [\"example\"], \"scopes\": [\"example\"]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_o_auth2_client_request_t *model = axiam_mgmt_create_o_auth2_client_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_o_auth2_client_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_o_auth2_client_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateOAuth2ClientRequest`: an EMPTY object parses without crashing. A server that omits
 * a field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_o_auth2_client_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_o_auth2_client_request_t *model = axiam_mgmt_create_o_auth2_client_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_o_auth2_client_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_o_auth2_client_request_free(model);
    cJSON_Delete(src);
}

static void test_create_o_auth2_client_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_o_auth2_client_request_parse(scalar));
    axiam_mgmt_create_o_auth2_client_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreatePermissionRequest`: a full wire object parses, builds back and frees. */
static void test_create_permission_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"action\": \"example\", \"description\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_permission_request_t *model = axiam_mgmt_create_permission_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_permission_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_permission_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreatePermissionRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_permission_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_permission_request_t *model = axiam_mgmt_create_permission_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_permission_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_permission_request_free(model);
    cJSON_Delete(src);
}

static void test_create_permission_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_permission_request_parse(scalar));
    axiam_mgmt_create_permission_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreatePgpKeyRequest`: a full wire object parses, builds back and frees. */
static void test_create_pgp_key_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"algorithm\": \"Rsa4096\", \"email\": \"example\", \"name\": \"example\", \"purpose\": \"AuditSigning\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_pgp_key_request_t *model = axiam_mgmt_create_pgp_key_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_pgp_key_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_pgp_key_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreatePgpKeyRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_pgp_key_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_pgp_key_request_t *model = axiam_mgmt_create_pgp_key_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_pgp_key_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_pgp_key_request_free(model);
    cJSON_Delete(src);
}

static void test_create_pgp_key_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_pgp_key_request_parse(scalar));
    axiam_mgmt_create_pgp_key_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateReactorRequest`: a full wire object parses, builds back and frees. */
static void test_create_reactor_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"enabled\": true, \"events\": [\"example\"], \"failure_policy\": \"fail_closed\", \"mode\": \"intercept\", \"name\": \"example\", \"priority\": 1, \"timeout_ms\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_reactor_request_t *model = axiam_mgmt_create_reactor_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_reactor_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_reactor_request_free(model);
    cJSON_Delete(src);
}

/* `CreateReactorRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_reactor_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"events\": [\"example\"], \"mode\": \"intercept\", \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_reactor_request_t *model = axiam_mgmt_create_reactor_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_reactor_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_reactor_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateReactorRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_reactor_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_reactor_request_t *model = axiam_mgmt_create_reactor_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_reactor_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_reactor_request_free(model);
    cJSON_Delete(src);
}

static void test_create_reactor_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_reactor_request_parse(scalar));
    axiam_mgmt_create_reactor_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateResourceRequest`: a full wire object parses, builds back and frees. */
static void test_create_resource_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"metadata\": {}, \"name\": \"example\", \"parent_id\": \"11111111-1111-4111-8111-111111111111\", \"resource_type\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_resource_request_t *model = axiam_mgmt_create_resource_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_resource_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_resource_request_free(model);
    cJSON_Delete(src);
}

/* `CreateResourceRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_resource_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"name\": \"example\", \"resource_type\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_resource_request_t *model = axiam_mgmt_create_resource_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_resource_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_resource_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateResourceRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_resource_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_resource_request_t *model = axiam_mgmt_create_resource_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_resource_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_resource_request_free(model);
    cJSON_Delete(src);
}

static void test_create_resource_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_resource_request_parse(scalar));
    axiam_mgmt_create_resource_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateRoleRequest`: a full wire object parses, builds back and frees. */
static void test_create_role_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"is_global\": true, \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_role_request_t *model = axiam_mgmt_create_role_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_role_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_role_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateRoleRequest`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_create_role_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_role_request_t *model = axiam_mgmt_create_role_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_role_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_role_request_free(model);
    cJSON_Delete(src);
}

static void test_create_role_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_role_request_parse(scalar));
    axiam_mgmt_create_role_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateScimTokenRequest`: a full wire object parses, builds back and frees. */
static void test_create_scim_token_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"expires_in_days\": 1, \"name\": \"example\", \"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_scim_token_request_t *model = axiam_mgmt_create_scim_token_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_scim_token_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_scim_token_request_free(model);
    cJSON_Delete(src);
}

/* `CreateScimTokenRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_scim_token_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"name\": \"example\", \"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_scim_token_request_t *model = axiam_mgmt_create_scim_token_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_scim_token_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_scim_token_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateScimTokenRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_scim_token_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_scim_token_request_t *model = axiam_mgmt_create_scim_token_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_scim_token_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_scim_token_request_free(model);
    cJSON_Delete(src);
}

static void test_create_scim_token_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_scim_token_request_parse(scalar));
    axiam_mgmt_create_scim_token_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateScimTokenResponse`: a full wire object parses, builds back and frees. */
static void test_create_scim_token_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"created_by\": \"11111111-1111-4111-8111-111111111111\", \"expires_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"last_used_at\": \"2026-08-26T00:00:00Z\", \"name\": \"example\", \"revoked_at\": \"2026-08-26T00:00:00Z\", \"status\": \"active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"user_id\": \"11111111-1111-4111-8111-111111111111\", \"provisioning_token\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_scim_token_response_t *model = axiam_mgmt_create_scim_token_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_scim_token_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_scim_token_response_free(model);
    cJSON_Delete(src);
}

/* `CreateScimTokenResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_create_scim_token_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"created_by\": \"11111111-1111-4111-8111-111111111111\", \"expires_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"status\": \"active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"user_id\": \"11111111-1111-4111-8111-111111111111\", \"provisioning_token\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_scim_token_response_t *model = axiam_mgmt_create_scim_token_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_scim_token_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_scim_token_response_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateScimTokenResponse`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_scim_token_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_scim_token_response_t *model = axiam_mgmt_create_scim_token_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_scim_token_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_scim_token_response_free(model);
    cJSON_Delete(src);
}

static void test_create_scim_token_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_scim_token_response_parse(scalar));
    axiam_mgmt_create_scim_token_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateScopeRequest`: a full wire object parses, builds back and frees. */
static void test_create_scope_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_scope_request_t *model = axiam_mgmt_create_scope_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_scope_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_scope_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateScopeRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_scope_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_scope_request_t *model = axiam_mgmt_create_scope_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_scope_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_scope_request_free(model);
    cJSON_Delete(src);
}

static void test_create_scope_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_scope_request_parse(scalar));
    axiam_mgmt_create_scope_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateServiceAccountRequest`: a full wire object parses, builds back and frees. */
static void test_create_service_account_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_service_account_request_t *model = axiam_mgmt_create_service_account_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_service_account_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_service_account_request_free(model);
    cJSON_Delete(src);
}

/* `CreateServiceAccountRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_service_account_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_service_account_request_t *model = axiam_mgmt_create_service_account_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_service_account_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_service_account_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateServiceAccountRequest`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_service_account_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_service_account_request_t *model = axiam_mgmt_create_service_account_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_service_account_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_service_account_request_free(model);
    cJSON_Delete(src);
}

static void test_create_service_account_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_service_account_request_parse(scalar));
    axiam_mgmt_create_service_account_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateTenantRequest`: a full wire object parses, builds back and frees. */
static void test_create_tenant_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"metadata\": {}, \"name\": \"example\", \"slug\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_tenant_request_t *model = axiam_mgmt_create_tenant_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_tenant_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_tenant_request_free(model);
    cJSON_Delete(src);
}

/* `CreateTenantRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_tenant_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"name\": \"example\", \"slug\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_tenant_request_t *model = axiam_mgmt_create_tenant_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_tenant_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_tenant_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateTenantRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_tenant_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_tenant_request_t *model = axiam_mgmt_create_tenant_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_tenant_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_tenant_request_free(model);
    cJSON_Delete(src);
}

static void test_create_tenant_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_tenant_request_parse(scalar));
    axiam_mgmt_create_tenant_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateUserRequest`: a full wire object parses, builds back and frees. */
static void test_create_user_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"email\": \"example\", \"metadata\": {}, \"opaque\": {\"opaque_session\": \"example\", \"registration_record\": \"example\"}, \"password\": \"example\", \"username\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_user_request_t *model = axiam_mgmt_create_user_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_user_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_user_request_free(model);
    cJSON_Delete(src);
}

/* `CreateUserRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_user_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"email\": \"example\", \"password\": \"example\", \"username\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_user_request_t *model = axiam_mgmt_create_user_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_user_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_user_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateUserRequest`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_create_user_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_user_request_t *model = axiam_mgmt_create_user_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_user_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_user_request_free(model);
    cJSON_Delete(src);
}

static void test_create_user_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_user_request_parse(scalar));
    axiam_mgmt_create_user_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `CreateWebhookRequest`: a full wire object parses, builds back and frees. */
static void test_create_webhook_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"events\": [\"example\"], \"retry_policy\": {\"backoff_multiplier\": 1.5, \"initial_delay_secs\": 1, \"max_retries\": 1}, \"secret\": \"example\", \"url\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_webhook_request_t *model = axiam_mgmt_create_webhook_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_webhook_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_webhook_request_free(model);
    cJSON_Delete(src);
}

/* `CreateWebhookRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_create_webhook_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"events\": [\"example\"], \"secret\": \"example\", \"url\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_webhook_request_t *model = axiam_mgmt_create_webhook_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_create_webhook_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_create_webhook_request_free(model);
    cJSON_Delete(src);
}

/*
 * `CreateWebhookRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_create_webhook_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_create_webhook_request_t *model = axiam_mgmt_create_webhook_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_create_webhook_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_create_webhook_request_free(model);
    cJSON_Delete(src);
}

static void test_create_webhook_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_create_webhook_request_parse(scalar));
    axiam_mgmt_create_webhook_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `EmailConfig`: a full wire object parses, builds back and frees. */
static void test_email_config_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"enabled\": true, \"from_email\": \"example\", \"from_name\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"provider\": {}, \"reply_to\": \"example\", \"scope\": \"Org\", \"scope_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_config_t *model = axiam_mgmt_email_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_email_config_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_email_config_free(model);
    cJSON_Delete(src);
}

/* `EmailConfig`: the server omitting every OPTIONAL field is not an error. */
static void test_email_config_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"enabled\": true, \"from_email\": \"example\", \"from_name\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"provider\": {}, \"scope\": \"Org\", \"scope_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_config_t *model = axiam_mgmt_email_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_email_config_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_email_config_free(model);
    cJSON_Delete(src);
}

/*
 * `EmailConfig`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_email_config_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_config_t *model = axiam_mgmt_email_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_email_config_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_email_config_free(model);
    cJSON_Delete(src);
}

static void test_email_config_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_email_config_parse(scalar));
    axiam_mgmt_email_config_free(NULL);
    cJSON_Delete(scalar);
}

/* `EmailConfigOverride`: a full wire object parses, builds back and frees. */
static void test_email_config_override_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"enabled\": true, \"from_email\": \"example\", \"from_name\": \"example\", \"provider\": {}, \"reply_to\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_config_override_t *model = axiam_mgmt_email_config_override_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_email_config_override_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_email_config_override_free(model);
    cJSON_Delete(src);
}

/* `EmailConfigOverride`: the server omitting every OPTIONAL field is not an error. */
static void test_email_config_override_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_config_override_t *model = axiam_mgmt_email_config_override_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_email_config_override_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_email_config_override_free(model);
    cJSON_Delete(src);
}

/*
 * `EmailConfigOverride`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_email_config_override_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_config_override_t *model = axiam_mgmt_email_config_override_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_email_config_override_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_email_config_override_free(model);
    cJSON_Delete(src);
}

static void test_email_config_override_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_email_config_override_parse(scalar));
    axiam_mgmt_email_config_override_free(NULL);
    cJSON_Delete(scalar);
}

/* `EmailTestResult`: a full wire object parses, builds back and frees. */
static void test_email_test_result_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"message_id\": \"example\", \"provider\": \"example\", \"to\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_test_result_t *model = axiam_mgmt_email_test_result_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_email_test_result_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_email_test_result_free(model);
    cJSON_Delete(src);
}

/* `EmailTestResult`: the server omitting every OPTIONAL field is not an error. */
static void test_email_test_result_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"provider\": \"example\", \"to\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_test_result_t *model = axiam_mgmt_email_test_result_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_email_test_result_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_email_test_result_free(model);
    cJSON_Delete(src);
}

/*
 * `EmailTestResult`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_email_test_result_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_test_result_t *model = axiam_mgmt_email_test_result_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_email_test_result_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_email_test_result_free(model);
    cJSON_Delete(src);
}

static void test_email_test_result_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_email_test_result_parse(scalar));
    axiam_mgmt_email_test_result_free(NULL);
    cJSON_Delete(scalar);
}

/* `EmailVerificationPolicy`: a full wire object parses, builds back and frees. */
static void test_email_verification_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"email_verification_grace_period_hours\": 1, \"email_verification_required\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_verification_policy_t *model = axiam_mgmt_email_verification_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_email_verification_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_email_verification_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `EmailVerificationPolicy`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_email_verification_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_email_verification_policy_t *model = axiam_mgmt_email_verification_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_email_verification_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_email_verification_policy_free(model);
    cJSON_Delete(src);
}

static void test_email_verification_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_email_verification_policy_parse(scalar));
    axiam_mgmt_email_verification_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `EncryptRequest`: a full wire object parses, builds back and frees. */
static void test_encrypt_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"data_base64\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_encrypt_request_t *model = axiam_mgmt_encrypt_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_encrypt_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_encrypt_request_free(model);
    cJSON_Delete(src);
}

/*
 * `EncryptRequest`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_encrypt_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_encrypt_request_t *model = axiam_mgmt_encrypt_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_encrypt_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_encrypt_request_free(model);
    cJSON_Delete(src);
}

static void test_encrypt_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_encrypt_request_parse(scalar));
    axiam_mgmt_encrypt_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `EncryptedExport`: a full wire object parses, builds back and frees. */
static void test_encrypted_export_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"ciphertext_armored\": \"example\", \"recipient_key_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_encrypted_export_t *model = axiam_mgmt_encrypted_export_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_encrypted_export_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_encrypted_export_free(model);
    cJSON_Delete(src);
}

/*
 * `EncryptedExport`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_encrypted_export_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_encrypted_export_t *model = axiam_mgmt_encrypted_export_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_encrypted_export_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_encrypted_export_free(model);
    cJSON_Delete(src);
}

static void test_encrypted_export_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_encrypted_export_parse(scalar));
    axiam_mgmt_encrypted_export_free(NULL);
    cJSON_Delete(scalar);
}

/* `FederationConfigResponse`: a full wire object parses, builds back and frees. */
static void test_federation_config_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"attribute_map\": {}, \"client_id\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"enabled\": true, \"id\": \"11111111-1111-4111-8111-111111111111\", \"metadata_url\": \"example\", \"protocol\": \"example\", \"provider\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"token_exchange\": {\"accepted_audiences\": [\"example\"], \"enabled\": true, \"max_lifetime_secs\": 1, \"max_token_age_secs\": 1, \"scope_map\": {}, \"subject_mapping\": \"example\"}, \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_federation_config_response_t *model = axiam_mgmt_federation_config_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_federation_config_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_federation_config_response_free(model);
    cJSON_Delete(src);
}

/* `FederationConfigResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_federation_config_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"attribute_map\": {}, \"client_id\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"enabled\": true, \"id\": \"11111111-1111-4111-8111-111111111111\", \"protocol\": \"example\", \"provider\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"token_exchange\": {\"accepted_audiences\": [\"example\"], \"enabled\": true, \"max_lifetime_secs\": 1, \"max_token_age_secs\": 1, \"scope_map\": {}, \"subject_mapping\": \"example\"}, \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_federation_config_response_t *model = axiam_mgmt_federation_config_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_federation_config_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_federation_config_response_free(model);
    cJSON_Delete(src);
}

/*
 * `FederationConfigResponse`: an EMPTY object parses without crashing. A server that omits
 * a field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_federation_config_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_federation_config_response_t *model = axiam_mgmt_federation_config_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_federation_config_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_federation_config_response_free(model);
    cJSON_Delete(src);
}

static void test_federation_config_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_federation_config_response_parse(scalar));
    axiam_mgmt_federation_config_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `FederationLinkResponse`: a full wire object parses, builds back and frees. */
static void test_federation_link_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"external_email\": \"example\", \"external_subject\": \"example\", \"federation_config_id\": \"11111111-1111-4111-8111-111111111111\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\", \"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_federation_link_response_t *model = axiam_mgmt_federation_link_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_federation_link_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_federation_link_response_free(model);
    cJSON_Delete(src);
}

/* `FederationLinkResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_federation_link_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"external_subject\": \"example\", \"federation_config_id\": \"11111111-1111-4111-8111-111111111111\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\", \"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_federation_link_response_t *model = axiam_mgmt_federation_link_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_federation_link_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_federation_link_response_free(model);
    cJSON_Delete(src);
}

/*
 * `FederationLinkResponse`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_federation_link_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_federation_link_response_t *model = axiam_mgmt_federation_link_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_federation_link_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_federation_link_response_free(model);
    cJSON_Delete(src);
}

static void test_federation_link_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_federation_link_response_parse(scalar));
    axiam_mgmt_federation_link_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `GeneratedCaCertificate`: a full wire object parses, builds back and frees. */
static void test_generated_ca_certificate_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"chain_pem\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"fingerprint\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"key_algorithm\": \"Rsa4096\", \"key_custody\": \"example\", \"key_locator\": \"example\", \"mtls_trust_anchor\": true, \"not_after\": \"2026-08-26T00:00:00Z\", \"not_before\": \"2026-08-26T00:00:00Z\", \"organization_id\": \"11111111-1111-4111-8111-111111111111\", \"parent_ca_id\": \"11111111-1111-4111-8111-111111111111\", \"public_cert_pem\": \"example\", \"status\": \"Active\", \"subject\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"private_key_pem\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_generated_ca_certificate_t *model = axiam_mgmt_generated_ca_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_generated_ca_certificate_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_generated_ca_certificate_free(model);
    cJSON_Delete(src);
}

/* `GeneratedCaCertificate`: the server omitting every OPTIONAL field is not an error. */
static void test_generated_ca_certificate_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"fingerprint\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"key_algorithm\": \"Rsa4096\", \"not_after\": \"2026-08-26T00:00:00Z\", \"not_before\": \"2026-08-26T00:00:00Z\", \"organization_id\": \"11111111-1111-4111-8111-111111111111\", \"public_cert_pem\": \"example\", \"status\": \"Active\", \"subject\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_generated_ca_certificate_t *model = axiam_mgmt_generated_ca_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_generated_ca_certificate_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_generated_ca_certificate_free(model);
    cJSON_Delete(src);
}

/*
 * `GeneratedCaCertificate`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_generated_ca_certificate_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_generated_ca_certificate_t *model = axiam_mgmt_generated_ca_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_generated_ca_certificate_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_generated_ca_certificate_free(model);
    cJSON_Delete(src);
}

static void test_generated_ca_certificate_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_generated_ca_certificate_parse(scalar));
    axiam_mgmt_generated_ca_certificate_free(NULL);
    cJSON_Delete(scalar);
}

/* `GeneratedCertificate`: a full wire object parses, builds back and frees. */
static void test_generated_certificate_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"cert_type\": \"User\", \"created_at\": \"2026-08-26T00:00:00Z\", \"fingerprint\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"issuer_ca_id\": \"11111111-1111-4111-8111-111111111111\", \"key_algorithm\": \"Rsa4096\", \"metadata\": {}, \"not_after\": \"2026-08-26T00:00:00Z\", \"not_before\": \"2026-08-26T00:00:00Z\", \"public_cert_pem\": \"example\", \"status\": \"Active\", \"subject\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"chain_pem\": \"example\", \"private_key_pem\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_generated_certificate_t *model = axiam_mgmt_generated_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_generated_certificate_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_generated_certificate_free(model);
    cJSON_Delete(src);
}

/* `GeneratedCertificate`: the server omitting every OPTIONAL field is not an error. */
static void test_generated_certificate_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"cert_type\": \"User\", \"created_at\": \"2026-08-26T00:00:00Z\", \"fingerprint\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"issuer_ca_id\": \"11111111-1111-4111-8111-111111111111\", \"key_algorithm\": \"Rsa4096\", \"metadata\": {}, \"not_after\": \"2026-08-26T00:00:00Z\", \"not_before\": \"2026-08-26T00:00:00Z\", \"public_cert_pem\": \"example\", \"status\": \"Active\", \"subject\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"private_key_pem\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_generated_certificate_t *model = axiam_mgmt_generated_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_generated_certificate_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_generated_certificate_free(model);
    cJSON_Delete(src);
}

/*
 * `GeneratedCertificate`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_generated_certificate_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_generated_certificate_t *model = axiam_mgmt_generated_certificate_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_generated_certificate_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_generated_certificate_free(model);
    cJSON_Delete(src);
}

static void test_generated_certificate_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_generated_certificate_parse(scalar));
    axiam_mgmt_generated_certificate_free(NULL);
    cJSON_Delete(scalar);
}

/* `GeneratedPgpKey`: a full wire object parses, builds back and frees. */
static void test_generated_pgp_key_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"algorithm\": \"Rsa4096\", \"created_at\": \"2026-08-26T00:00:00Z\", \"fingerprint\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"public_key_armored\": \"example\", \"purpose\": \"AuditSigning\", \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"private_key_armored\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_generated_pgp_key_t *model = axiam_mgmt_generated_pgp_key_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_generated_pgp_key_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_generated_pgp_key_free(model);
    cJSON_Delete(src);
}

/* `GeneratedPgpKey`: the server omitting every OPTIONAL field is not an error. */
static void test_generated_pgp_key_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"algorithm\": \"Rsa4096\", \"created_at\": \"2026-08-26T00:00:00Z\", \"fingerprint\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"public_key_armored\": \"example\", \"purpose\": \"AuditSigning\", \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_generated_pgp_key_t *model = axiam_mgmt_generated_pgp_key_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_generated_pgp_key_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_generated_pgp_key_free(model);
    cJSON_Delete(src);
}

/*
 * `GeneratedPgpKey`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_generated_pgp_key_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_generated_pgp_key_t *model = axiam_mgmt_generated_pgp_key_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_generated_pgp_key_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_generated_pgp_key_free(model);
    cJSON_Delete(src);
}

static void test_generated_pgp_key_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_generated_pgp_key_parse(scalar));
    axiam_mgmt_generated_pgp_key_free(NULL);
    cJSON_Delete(scalar);
}

/* `GrantPermissionRequest`: a full wire object parses, builds back and frees. */
static void test_grant_permission_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"effect\": \"allow\", \"permission_id\": \"11111111-1111-4111-8111-111111111111\", \"scope_ids\": [\"11111111-1111-4111-8111-111111111111\"]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_grant_permission_request_t *model = axiam_mgmt_grant_permission_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_grant_permission_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_grant_permission_request_free(model);
    cJSON_Delete(src);
}

/* `GrantPermissionRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_grant_permission_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"permission_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_grant_permission_request_t *model = axiam_mgmt_grant_permission_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_grant_permission_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_grant_permission_request_free(model);
    cJSON_Delete(src);
}

/*
 * `GrantPermissionRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_grant_permission_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_grant_permission_request_t *model = axiam_mgmt_grant_permission_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_grant_permission_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_grant_permission_request_free(model);
    cJSON_Delete(src);
}

static void test_grant_permission_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_grant_permission_request_parse(scalar));
    axiam_mgmt_grant_permission_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `GrantedScope`: a full wire object parses, builds back and frees. */
static void test_granted_scope_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"resource_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_granted_scope_t *model = axiam_mgmt_granted_scope_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_granted_scope_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_granted_scope_free(model);
    cJSON_Delete(src);
}

/*
 * `GrantedScope`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_granted_scope_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_granted_scope_t *model = axiam_mgmt_granted_scope_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_granted_scope_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_granted_scope_free(model);
    cJSON_Delete(src);
}

static void test_granted_scope_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_granted_scope_parse(scalar));
    axiam_mgmt_granted_scope_free(NULL);
    cJSON_Delete(scalar);
}

/* `Group`: a full wire object parses, builds back and frees. */
static void test_group_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"metadata\": {}, \"name\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_group_t *model = axiam_mgmt_group_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_group_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_group_free(model);
    cJSON_Delete(src);
}

/*
 * `Group`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_group_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_group_t *model = axiam_mgmt_group_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_group_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_group_free(model);
    cJSON_Delete(src);
}

static void test_group_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_group_parse(scalar));
    axiam_mgmt_group_free(NULL);
    cJSON_Delete(scalar);
}

/* `HealthResponse`: a full wire object parses, builds back and frees. */
static void test_health_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"status\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_health_response_t *model = axiam_mgmt_health_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_health_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_health_response_free(model);
    cJSON_Delete(src);
}

/*
 * `HealthResponse`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_health_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_health_response_t *model = axiam_mgmt_health_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_health_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_health_response_free(model);
    cJSON_Delete(src);
}

static void test_health_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_health_response_parse(scalar));
    axiam_mgmt_health_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `ImportCaCertificateRequest`: a full wire object parses, builds back and frees. */
static void test_import_ca_certificate_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"private_key_pem\": \"example\", \"public_cert_pem\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_import_ca_certificate_request_t *model = axiam_mgmt_import_ca_certificate_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_import_ca_certificate_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_import_ca_certificate_request_free(model);
    cJSON_Delete(src);
}

/* `ImportCaCertificateRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_import_ca_certificate_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"public_cert_pem\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_import_ca_certificate_request_t *model = axiam_mgmt_import_ca_certificate_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_import_ca_certificate_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_import_ca_certificate_request_free(model);
    cJSON_Delete(src);
}

/*
 * `ImportCaCertificateRequest`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_import_ca_certificate_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_import_ca_certificate_request_t *model = axiam_mgmt_import_ca_certificate_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_import_ca_certificate_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_import_ca_certificate_request_free(model);
    cJSON_Delete(src);
}

static void test_import_ca_certificate_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_import_ca_certificate_request_parse(scalar));
    axiam_mgmt_import_ca_certificate_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `LockoutPolicy`: a full wire object parses, builds back and frees. */
static void test_lockout_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"lockout_backoff_multiplier\": 1.5, \"lockout_duration_secs\": 1, \"max_failed_login_attempts\": 1, \"max_lockout_duration_secs\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_lockout_policy_t *model = axiam_mgmt_lockout_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_lockout_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_lockout_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `LockoutPolicy`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_lockout_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_lockout_policy_t *model = axiam_mgmt_lockout_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_lockout_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_lockout_policy_free(model);
    cJSON_Delete(src);
}

static void test_lockout_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_lockout_policy_parse(scalar));
    axiam_mgmt_lockout_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `MdsStatusResponse`: a full wire object parses, builds back and frees. */
static void test_mds_status_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"entry_count\": 1, \"last_refreshed_at\": \"2026-08-26T00:00:00Z\", \"next_update\": \"example\", \"no\": 1, \"stale\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mds_status_response_t *model = axiam_mgmt_mds_status_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_mds_status_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_mds_status_response_free(model);
    cJSON_Delete(src);
}

/* `MdsStatusResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_mds_status_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"entry_count\": 1, \"stale\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mds_status_response_t *model = axiam_mgmt_mds_status_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_mds_status_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_mds_status_response_free(model);
    cJSON_Delete(src);
}

/*
 * `MdsStatusResponse`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_mds_status_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mds_status_response_t *model = axiam_mgmt_mds_status_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_mds_status_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_mds_status_response_free(model);
    cJSON_Delete(src);
}

static void test_mds_status_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_mds_status_response_parse(scalar));
    axiam_mgmt_mds_status_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `MfaMethodResponse`: a full wire object parses, builds back and frees. */
static void test_mfa_method_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"example\", \"last_used_at\": \"example\", \"method_id\": \"example\", \"method_type\": \"Totp\", \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mfa_method_response_t *model = axiam_mgmt_mfa_method_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_mfa_method_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_mfa_method_response_free(model);
    cJSON_Delete(src);
}

/* `MfaMethodResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_mfa_method_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"example\", \"method_id\": \"example\", \"method_type\": \"Totp\", \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mfa_method_response_t *model = axiam_mgmt_mfa_method_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_mfa_method_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_mfa_method_response_free(model);
    cJSON_Delete(src);
}

/*
 * `MfaMethodResponse`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_mfa_method_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mfa_method_response_t *model = axiam_mgmt_mfa_method_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_mfa_method_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_mfa_method_response_free(model);
    cJSON_Delete(src);
}

static void test_mfa_method_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_mfa_method_response_parse(scalar));
    axiam_mgmt_mfa_method_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `MfaPolicy`: a full wire object parses, builds back and frees. */
static void test_mfa_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"mfa_challenge_lifetime_secs\": 1, \"mfa_enforced\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mfa_policy_t *model = axiam_mgmt_mfa_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_mfa_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_mfa_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `MfaPolicy`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_mfa_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mfa_policy_t *model = axiam_mgmt_mfa_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_mfa_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_mfa_policy_free(model);
    cJSON_Delete(src);
}

static void test_mfa_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_mfa_policy_parse(scalar));
    axiam_mgmt_mfa_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `MigrateCustodyResponse`: a full wire object parses, builds back and frees. */
static void test_migrate_custody_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"ca_certificate_id\": \"11111111-1111-4111-8111-111111111111\", \"key_custody\": \"example\", \"key_locator\": \"example\", \"previous_custody\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_migrate_custody_response_t *model = axiam_mgmt_migrate_custody_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_migrate_custody_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_migrate_custody_response_free(model);
    cJSON_Delete(src);
}

/* `MigrateCustodyResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_migrate_custody_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"ca_certificate_id\": \"11111111-1111-4111-8111-111111111111\", \"key_custody\": \"example\", \"previous_custody\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_migrate_custody_response_t *model = axiam_mgmt_migrate_custody_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_migrate_custody_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_migrate_custody_response_free(model);
    cJSON_Delete(src);
}

/*
 * `MigrateCustodyResponse`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_migrate_custody_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_migrate_custody_response_t *model = axiam_mgmt_migrate_custody_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_migrate_custody_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_migrate_custody_response_free(model);
    cJSON_Delete(src);
}

static void test_migrate_custody_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_migrate_custody_response_parse(scalar));
    axiam_mgmt_migrate_custody_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `MtlsTrustAnchorResponse`: a full wire object parses, builds back and frees. */
static void test_mtls_trust_anchor_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"ca_certificate_id\": \"11111111-1111-4111-8111-111111111111\", \"message\": \"example\", \"mtls_trust_anchor\": true, \"restart_required\": true, \"trusted_anchors\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mtls_trust_anchor_response_t *model = axiam_mgmt_mtls_trust_anchor_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_mtls_trust_anchor_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_mtls_trust_anchor_response_free(model);
    cJSON_Delete(src);
}

/* `MtlsTrustAnchorResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_mtls_trust_anchor_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"ca_certificate_id\": \"11111111-1111-4111-8111-111111111111\", \"message\": \"example\", \"mtls_trust_anchor\": true, \"restart_required\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mtls_trust_anchor_response_t *model = axiam_mgmt_mtls_trust_anchor_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_mtls_trust_anchor_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_mtls_trust_anchor_response_free(model);
    cJSON_Delete(src);
}

/*
 * `MtlsTrustAnchorResponse`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_mtls_trust_anchor_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_mtls_trust_anchor_response_t *model = axiam_mgmt_mtls_trust_anchor_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_mtls_trust_anchor_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_mtls_trust_anchor_response_free(model);
    cJSON_Delete(src);
}

static void test_mtls_trust_anchor_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_mtls_trust_anchor_response_parse(scalar));
    axiam_mgmt_mtls_trust_anchor_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `NotificationPolicy`: a full wire object parses, builds back and frees. */
static void test_notification_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"admin_notifications_enabled\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_notification_policy_t *model = axiam_mgmt_notification_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_notification_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_notification_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `NotificationPolicy`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_notification_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_notification_policy_t *model = axiam_mgmt_notification_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_notification_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_notification_policy_free(model);
    cJSON_Delete(src);
}

static void test_notification_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_notification_policy_parse(scalar));
    axiam_mgmt_notification_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `NotificationRuleResponse`: a full wire object parses, builds back and frees. */
static void test_notification_rule_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"enabled\": true, \"events\": [\"login_failure\"], \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"recipient_emails\": [\"example\"], \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_notification_rule_response_t *model = axiam_mgmt_notification_rule_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_notification_rule_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_notification_rule_response_free(model);
    cJSON_Delete(src);
}

/*
 * `NotificationRuleResponse`: an EMPTY object parses without crashing. A server that omits
 * a field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_notification_rule_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_notification_rule_response_t *model = axiam_mgmt_notification_rule_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_notification_rule_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_notification_rule_response_free(model);
    cJSON_Delete(src);
}

static void test_notification_rule_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_notification_rule_response_parse(scalar));
    axiam_mgmt_notification_rule_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `OAuth2ClientCreatedResponse`: a full wire object parses, builds back and frees. */
static void test_o_auth2_client_created_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"client_id\": \"example\", \"client_secret\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"grant_types\": [\"example\"], \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"redirect_uris\": [\"example\"], \"scopes\": [\"example\"], \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_o_auth2_client_created_response_t *model = axiam_mgmt_o_auth2_client_created_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_o_auth2_client_created_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_o_auth2_client_created_response_free(model);
    cJSON_Delete(src);
}

/*
 * `OAuth2ClientCreatedResponse`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_o_auth2_client_created_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_o_auth2_client_created_response_t *model = axiam_mgmt_o_auth2_client_created_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_o_auth2_client_created_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_o_auth2_client_created_response_free(model);
    cJSON_Delete(src);
}

static void test_o_auth2_client_created_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_o_auth2_client_created_response_parse(scalar));
    axiam_mgmt_o_auth2_client_created_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `OAuth2ClientResponse`: a full wire object parses, builds back and frees. */
static void test_o_auth2_client_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"client_id\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"dpop_bound_access_tokens\": true, \"dpop_require_nonce\": true, \"grant_types\": [\"example\"], \"id\": \"11111111-1111-4111-8111-111111111111\", \"jwks\": \"example\", \"jwks_uri\": \"example\", \"name\": \"example\", \"profile\": \"standard\", \"redirect_uris\": [\"example\"], \"require_par\": true, \"scopes\": [\"example\"], \"self_signed_tls_client_auth_thumbprints\": [\"example\"], \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"tls_client_auth_san_dns\": \"example\", \"tls_client_auth_san_uri\": \"example\", \"tls_client_auth_subject_dn\": \"example\", \"tls_client_certificate_bound_access_tokens\": true, \"token_endpoint_auth_method\": \"client_secret_post\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_o_auth2_client_response_t *model = axiam_mgmt_o_auth2_client_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_o_auth2_client_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_o_auth2_client_response_free(model);
    cJSON_Delete(src);
}

/* `OAuth2ClientResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_o_auth2_client_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"client_id\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"dpop_bound_access_tokens\": true, \"dpop_require_nonce\": true, \"grant_types\": [\"example\"], \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"profile\": \"standard\", \"redirect_uris\": [\"example\"], \"require_par\": true, \"scopes\": [\"example\"], \"self_signed_tls_client_auth_thumbprints\": [\"example\"], \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"tls_client_certificate_bound_access_tokens\": true, \"token_endpoint_auth_method\": \"client_secret_post\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_o_auth2_client_response_t *model = axiam_mgmt_o_auth2_client_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_o_auth2_client_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_o_auth2_client_response_free(model);
    cJSON_Delete(src);
}

/*
 * `OAuth2ClientResponse`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_o_auth2_client_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_o_auth2_client_response_t *model = axiam_mgmt_o_auth2_client_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_o_auth2_client_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_o_auth2_client_response_free(model);
    cJSON_Delete(src);
}

static void test_o_auth2_client_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_o_auth2_client_response_parse(scalar));
    axiam_mgmt_o_auth2_client_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `OidcAuthorizeRequest`: a full wire object parses, builds back and frees. */
static void test_oidc_authorize_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"config_id\": \"11111111-1111-4111-8111-111111111111\", \"nonce\": \"example\", \"redirect_uri\": \"example\", \"state\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_oidc_authorize_request_t *model = axiam_mgmt_oidc_authorize_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_oidc_authorize_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_oidc_authorize_request_free(model);
    cJSON_Delete(src);
}

/*
 * `OidcAuthorizeRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_oidc_authorize_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_oidc_authorize_request_t *model = axiam_mgmt_oidc_authorize_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_oidc_authorize_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_oidc_authorize_request_free(model);
    cJSON_Delete(src);
}

static void test_oidc_authorize_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_oidc_authorize_request_parse(scalar));
    axiam_mgmt_oidc_authorize_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `OidcAuthorizeResponse`: a full wire object parses, builds back and frees. */
static void test_oidc_authorize_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"url\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_oidc_authorize_response_t *model = axiam_mgmt_oidc_authorize_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_oidc_authorize_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_oidc_authorize_response_free(model);
    cJSON_Delete(src);
}

/*
 * `OidcAuthorizeResponse`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_oidc_authorize_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_oidc_authorize_response_t *model = axiam_mgmt_oidc_authorize_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_oidc_authorize_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_oidc_authorize_response_free(model);
    cJSON_Delete(src);
}

static void test_oidc_authorize_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_oidc_authorize_response_parse(scalar));
    axiam_mgmt_oidc_authorize_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `OidcCallbackRequest`: a full wire object parses, builds back and frees. */
static void test_oidc_callback_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"code\": \"example\", \"config_id\": \"11111111-1111-4111-8111-111111111111\", \"nonce\": \"example\", \"redirect_uri\": \"example\", \"state\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_oidc_callback_request_t *model = axiam_mgmt_oidc_callback_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_oidc_callback_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_oidc_callback_request_free(model);
    cJSON_Delete(src);
}

/*
 * `OidcCallbackRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_oidc_callback_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_oidc_callback_request_t *model = axiam_mgmt_oidc_callback_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_oidc_callback_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_oidc_callback_request_free(model);
    cJSON_Delete(src);
}

static void test_oidc_callback_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_oidc_callback_request_parse(scalar));
    axiam_mgmt_oidc_callback_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `OidcCallbackResponse`: a full wire object parses, builds back and frees. */
static void test_oidc_callback_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"federation_link_id\": \"11111111-1111-4111-8111-111111111111\", \"newly_provisioned\": true, \"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_oidc_callback_response_t *model = axiam_mgmt_oidc_callback_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_oidc_callback_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_oidc_callback_response_free(model);
    cJSON_Delete(src);
}

/*
 * `OidcCallbackResponse`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_oidc_callback_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_oidc_callback_response_t *model = axiam_mgmt_oidc_callback_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_oidc_callback_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_oidc_callback_response_free(model);
    cJSON_Delete(src);
}

static void test_oidc_callback_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_oidc_callback_response_parse(scalar));
    axiam_mgmt_oidc_callback_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `OpaqueEnrollment`: a full wire object parses, builds back and frees. */
static void test_opaque_enrollment_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"opaque_session\": \"example\", \"registration_record\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_opaque_enrollment_t *model = axiam_mgmt_opaque_enrollment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_opaque_enrollment_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_opaque_enrollment_free(model);
    cJSON_Delete(src);
}

/*
 * `OpaqueEnrollment`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_opaque_enrollment_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_opaque_enrollment_t *model = axiam_mgmt_opaque_enrollment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_opaque_enrollment_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_opaque_enrollment_free(model);
    cJSON_Delete(src);
}

static void test_opaque_enrollment_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_opaque_enrollment_parse(scalar));
    axiam_mgmt_opaque_enrollment_free(NULL);
    cJSON_Delete(scalar);
}

/* `OpaquePolicy`: a full wire object parses, builds back and frees. */
static void test_opaque_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"opaque_ksf\": \"example\", \"opaque_mode\": \"example\", \"opaque_suite\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_opaque_policy_t *model = axiam_mgmt_opaque_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_opaque_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_opaque_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `OpaquePolicy`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_opaque_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_opaque_policy_t *model = axiam_mgmt_opaque_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_opaque_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_opaque_policy_free(model);
    cJSON_Delete(src);
}

static void test_opaque_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_opaque_policy_parse(scalar));
    axiam_mgmt_opaque_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `Organization`: a full wire object parses, builds back and frees. */
static void test_organization_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"metadata\": {}, \"name\": \"example\", \"slug\": \"example\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_organization_t *model = axiam_mgmt_organization_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_organization_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_organization_free(model);
    cJSON_Delete(src);
}

/*
 * `Organization`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_organization_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_organization_t *model = axiam_mgmt_organization_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_organization_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_organization_free(model);
    cJSON_Delete(src);
}

static void test_organization_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_organization_parse(scalar));
    axiam_mgmt_organization_free(NULL);
    cJSON_Delete(scalar);
}

/* `PasswordPolicy`: a full wire object parses, builds back and frees. */
static void test_password_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"hibp_check_enabled\": true, \"min_length\": 1, \"password_history_count\": 1, \"require_digits\": true, \"require_lowercase\": true, \"require_symbols\": true, \"require_uppercase\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_password_policy_t *model = axiam_mgmt_password_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_password_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_password_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `PasswordPolicy`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_password_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_password_policy_t *model = axiam_mgmt_password_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_password_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_password_policy_free(model);
    cJSON_Delete(src);
}

static void test_password_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_password_policy_parse(scalar));
    axiam_mgmt_password_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `Permission`: a full wire object parses, builds back and frees. */
static void test_permission_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"action\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_permission_t *model = axiam_mgmt_permission_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_permission_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_permission_free(model);
    cJSON_Delete(src);
}

/*
 * `Permission`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_permission_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_permission_t *model = axiam_mgmt_permission_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_permission_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_permission_free(model);
    cJSON_Delete(src);
}

static void test_permission_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_permission_parse(scalar));
    axiam_mgmt_permission_free(NULL);
    cJSON_Delete(scalar);
}

/* `PgpKey`: a full wire object parses, builds back and frees. */
static void test_pgp_key_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"algorithm\": \"Rsa4096\", \"created_at\": \"2026-08-26T00:00:00Z\", \"fingerprint\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"public_key_armored\": \"example\", \"purpose\": \"AuditSigning\", \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_pgp_key_t *model = axiam_mgmt_pgp_key_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_pgp_key_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_pgp_key_free(model);
    cJSON_Delete(src);
}

/*
 * `PgpKey`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_pgp_key_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_pgp_key_t *model = axiam_mgmt_pgp_key_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_pgp_key_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_pgp_key_free(model);
    cJSON_Delete(src);
}

static void test_pgp_key_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_pgp_key_parse(scalar));
    axiam_mgmt_pgp_key_free(NULL);
    cJSON_Delete(scalar);
}

/* `PolicyResponse`: a full wire object parses, builds back and frees. */
static void test_policy_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"allowed_aaguids\": [\"11111111-1111-4111-8111-111111111111\"], \"block_revoked_status\": true, \"blocked_aaguids\": [\"11111111-1111-4111-8111-111111111111\"], \"min_certification\": \"L1\", \"mode\": \"none\", \"require_fido_certified\": true, \"unknown_aaguid\": \"allow\", \"effective_unknown_aaguid\": \"allow\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_policy_response_t *model = axiam_mgmt_policy_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_policy_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_policy_response_free(model);
    cJSON_Delete(src);
}

/* `PolicyResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_policy_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"block_revoked_status\": true, \"mode\": \"none\", \"require_fido_certified\": true, \"effective_unknown_aaguid\": \"allow\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_policy_response_t *model = axiam_mgmt_policy_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_policy_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_policy_response_free(model);
    cJSON_Delete(src);
}

/*
 * `PolicyResponse`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_policy_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_policy_response_t *model = axiam_mgmt_policy_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_policy_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_policy_response_free(model);
    cJSON_Delete(src);
}

static void test_policy_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_policy_response_parse(scalar));
    axiam_mgmt_policy_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `PrivacyPolicy`: a full wire object parses, builds back and frees. */
static void test_privacy_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"deletion_grace_period_days\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_privacy_policy_t *model = axiam_mgmt_privacy_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_privacy_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_privacy_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `PrivacyPolicy`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_privacy_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_privacy_policy_t *model = axiam_mgmt_privacy_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_privacy_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_privacy_policy_free(model);
    cJSON_Delete(src);
}

static void test_privacy_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_privacy_policy_parse(scalar));
    axiam_mgmt_privacy_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `ReactorEventDescriptor`: a full wire object parses, builds back and frees. */
static void test_reactor_event_descriptor_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"default_failure_policy\": \"fail_closed\", \"description\": \"example\", \"interceptable\": true, \"mutable\": true, \"mutable_fields\": [\"example\"], \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_reactor_event_descriptor_t *model = axiam_mgmt_reactor_event_descriptor_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_reactor_event_descriptor_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_reactor_event_descriptor_free(model);
    cJSON_Delete(src);
}

/*
 * `ReactorEventDescriptor`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_reactor_event_descriptor_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_reactor_event_descriptor_t *model = axiam_mgmt_reactor_event_descriptor_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_reactor_event_descriptor_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_reactor_event_descriptor_free(model);
    cJSON_Delete(src);
}

static void test_reactor_event_descriptor_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_reactor_event_descriptor_parse(scalar));
    axiam_mgmt_reactor_event_descriptor_free(NULL);
    cJSON_Delete(scalar);
}

/* `ReactorResponse`: a full wire object parses, builds back and frees. */
static void test_reactor_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"enabled\": true, \"events\": [\"example\"], \"failure_policy\": \"fail_closed\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"last_seen_at\": \"2026-08-26T00:00:00Z\", \"mode\": \"intercept\", \"name\": \"example\", \"priority\": 1, \"recent_timeout_count\": 1, \"recent_veto_count\": 1, \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"timeout_ms\": 1, \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_reactor_response_t *model = axiam_mgmt_reactor_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_reactor_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_reactor_response_free(model);
    cJSON_Delete(src);
}

/* `ReactorResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_reactor_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"enabled\": true, \"events\": [\"example\"], \"failure_policy\": \"fail_closed\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"mode\": \"intercept\", \"name\": \"example\", \"priority\": 1, \"recent_timeout_count\": 1, \"recent_veto_count\": 1, \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"timeout_ms\": 1, \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_reactor_response_t *model = axiam_mgmt_reactor_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_reactor_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_reactor_response_free(model);
    cJSON_Delete(src);
}

/*
 * `ReactorResponse`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_reactor_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_reactor_response_t *model = axiam_mgmt_reactor_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_reactor_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_reactor_response_free(model);
    cJSON_Delete(src);
}

static void test_reactor_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_reactor_response_parse(scalar));
    axiam_mgmt_reactor_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `ReadyResponse`: a full wire object parses, builds back and frees. */
static void test_ready_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"database\": \"example\", \"status\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_ready_response_t *model = axiam_mgmt_ready_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_ready_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_ready_response_free(model);
    cJSON_Delete(src);
}

/*
 * `ReadyResponse`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_ready_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_ready_response_t *model = axiam_mgmt_ready_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_ready_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_ready_response_free(model);
    cJSON_Delete(src);
}

static void test_ready_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_ready_response_parse(scalar));
    axiam_mgmt_ready_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `ResolvedPermissionGrant`: a full wire object parses, builds back and frees. */
static void test_resolved_permission_grant_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"effect\": \"allow\", \"permission\": {\"action\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}, \"scope_ids\": [\"11111111-1111-4111-8111-111111111111\"], \"scopes\": [{\"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"resource_id\": \"11111111-1111-4111-8111-111111111111\"}]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_resolved_permission_grant_t *model = axiam_mgmt_resolved_permission_grant_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_resolved_permission_grant_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_resolved_permission_grant_free(model);
    cJSON_Delete(src);
}

/*
 * `ResolvedPermissionGrant`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_resolved_permission_grant_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_resolved_permission_grant_t *model = axiam_mgmt_resolved_permission_grant_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_resolved_permission_grant_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_resolved_permission_grant_free(model);
    cJSON_Delete(src);
}

static void test_resolved_permission_grant_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_resolved_permission_grant_parse(scalar));
    axiam_mgmt_resolved_permission_grant_free(NULL);
    cJSON_Delete(scalar);
}

/* `Resource`: a full wire object parses, builds back and frees. */
static void test_resource_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"metadata\": {}, \"name\": \"example\", \"parent_id\": \"11111111-1111-4111-8111-111111111111\", \"resource_type\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"uma_registered_by\": \"example\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_resource_t *model = axiam_mgmt_resource_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_resource_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_resource_free(model);
    cJSON_Delete(src);
}

/* `Resource`: the server omitting every OPTIONAL field is not an error. */
static void test_resource_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"metadata\": {}, \"name\": \"example\", \"resource_type\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_resource_t *model = axiam_mgmt_resource_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_resource_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_resource_free(model);
    cJSON_Delete(src);
}

/*
 * `Resource`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_resource_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_resource_t *model = axiam_mgmt_resource_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_resource_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_resource_free(model);
    cJSON_Delete(src);
}

static void test_resource_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_resource_parse(scalar));
    axiam_mgmt_resource_free(NULL);
    cJSON_Delete(scalar);
}

/* `RetryPolicy`: a full wire object parses, builds back and frees. */
static void test_retry_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"backoff_multiplier\": 1.5, \"initial_delay_secs\": 1, \"max_retries\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_retry_policy_t *model = axiam_mgmt_retry_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_retry_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_retry_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `RetryPolicy`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_retry_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_retry_policy_t *model = axiam_mgmt_retry_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_retry_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_retry_policy_free(model);
    cJSON_Delete(src);
}

static void test_retry_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_retry_policy_parse(scalar));
    axiam_mgmt_retry_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `Role`: a full wire object parses, builds back and frees. */
static void test_role_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"is_global\": true, \"name\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_t *model = axiam_mgmt_role_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_role_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_role_free(model);
    cJSON_Delete(src);
}

/*
 * `Role`: an EMPTY object parses without crashing. A server that omits a field openapi.json
 * marks required is malformed, and this SDK's answer is a model with that member unset
 * rather than an abort -- the caller is in a position to decide, and a parser that
 * segfaults on a bad response is a worse failure than one that hands back a null. This also
 * reaches the ABSENT arm of every field guard, required ones included, which no well-formed
 * fixture can.
 */
static void test_role_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_t *model = axiam_mgmt_role_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_role_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_role_free(model);
    cJSON_Delete(src);
}

static void test_role_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_role_parse(scalar));
    axiam_mgmt_role_free(NULL);
    cJSON_Delete(scalar);
}

/* `RoleAssignment`: a full wire object parses, builds back and frees. */
static void test_role_assignment_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"resource_id\": \"11111111-1111-4111-8111-111111111111\", \"role\": {\"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"is_global\": true, \"name\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}, \"tenant_scope\": [\"11111111-1111-4111-8111-111111111111\"]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_assignment_t *model = axiam_mgmt_role_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_role_assignment_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_role_assignment_free(model);
    cJSON_Delete(src);
}

/* `RoleAssignment`: the server omitting every OPTIONAL field is not an error. */
static void test_role_assignment_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"role\": {\"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"is_global\": true, \"name\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_assignment_t *model = axiam_mgmt_role_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_role_assignment_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_role_assignment_free(model);
    cJSON_Delete(src);
}

/*
 * `RoleAssignment`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_role_assignment_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_assignment_t *model = axiam_mgmt_role_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_role_assignment_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_role_assignment_free(model);
    cJSON_Delete(src);
}

static void test_role_assignment_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_role_assignment_parse(scalar));
    axiam_mgmt_role_assignment_free(NULL);
    cJSON_Delete(scalar);
}

/* `RoleGroupAssignment`: a full wire object parses, builds back and frees. */
static void test_role_group_assignment_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"group\": {\"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"metadata\": {}, \"name\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}, \"resource_id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_scope\": [\"11111111-1111-4111-8111-111111111111\"]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_group_assignment_t *model = axiam_mgmt_role_group_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_role_group_assignment_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_role_group_assignment_free(model);
    cJSON_Delete(src);
}

/* `RoleGroupAssignment`: the server omitting every OPTIONAL field is not an error. */
static void test_role_group_assignment_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"group\": {\"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"metadata\": {}, \"name\": \"example\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_group_assignment_t *model = axiam_mgmt_role_group_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_role_group_assignment_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_role_group_assignment_free(model);
    cJSON_Delete(src);
}

/*
 * `RoleGroupAssignment`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_role_group_assignment_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_group_assignment_t *model = axiam_mgmt_role_group_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_role_group_assignment_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_role_group_assignment_free(model);
    cJSON_Delete(src);
}

static void test_role_group_assignment_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_role_group_assignment_parse(scalar));
    axiam_mgmt_role_group_assignment_free(NULL);
    cJSON_Delete(scalar);
}

/* `RoleServiceAccountAssignment`: a full wire object parses, builds back and frees. */
static void test_role_service_account_assignment_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"resource_id\": \"11111111-1111-4111-8111-111111111111\", \"service_account\": {\"client_id\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}, \"tenant_scope\": [\"11111111-1111-4111-8111-111111111111\"]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_service_account_assignment_t *model = axiam_mgmt_role_service_account_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_role_service_account_assignment_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_role_service_account_assignment_free(model);
    cJSON_Delete(src);
}

/* `RoleServiceAccountAssignment`: the server omitting every OPTIONAL field is not an error. */
static void test_role_service_account_assignment_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"service_account\": {\"client_id\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_service_account_assignment_t *model = axiam_mgmt_role_service_account_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_role_service_account_assignment_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_role_service_account_assignment_free(model);
    cJSON_Delete(src);
}

/*
 * `RoleServiceAccountAssignment`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_role_service_account_assignment_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_service_account_assignment_t *model = axiam_mgmt_role_service_account_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_role_service_account_assignment_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_role_service_account_assignment_free(model);
    cJSON_Delete(src);
}

static void test_role_service_account_assignment_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_role_service_account_assignment_parse(scalar));
    axiam_mgmt_role_service_account_assignment_free(NULL);
    cJSON_Delete(scalar);
}

/* `RoleUserAssignment`: a full wire object parses, builds back and frees. */
static void test_role_user_assignment_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"resource_id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_scope\": [\"11111111-1111-4111-8111-111111111111\"], \"user\": {\"created_at\": \"2026-08-26T00:00:00Z\", \"email\": \"example\", \"email_verified\": true, \"failed_login_attempts\": 1, \"id\": \"11111111-1111-4111-8111-111111111111\", \"is_locked\": true, \"locked_until\": \"2026-08-26T00:00:00Z\", \"metadata\": {}, \"mfa_enabled\": true, \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\", \"username\": \"example\"}}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_user_assignment_t *model = axiam_mgmt_role_user_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_role_user_assignment_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_role_user_assignment_free(model);
    cJSON_Delete(src);
}

/* `RoleUserAssignment`: the server omitting every OPTIONAL field is not an error. */
static void test_role_user_assignment_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"user\": {\"created_at\": \"2026-08-26T00:00:00Z\", \"email\": \"example\", \"email_verified\": true, \"failed_login_attempts\": 1, \"id\": \"11111111-1111-4111-8111-111111111111\", \"is_locked\": true, \"locked_until\": \"2026-08-26T00:00:00Z\", \"metadata\": {}, \"mfa_enabled\": true, \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\", \"username\": \"example\"}}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_user_assignment_t *model = axiam_mgmt_role_user_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_role_user_assignment_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_role_user_assignment_free(model);
    cJSON_Delete(src);
}

/*
 * `RoleUserAssignment`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_role_user_assignment_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_role_user_assignment_t *model = axiam_mgmt_role_user_assignment_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_role_user_assignment_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_role_user_assignment_free(model);
    cJSON_Delete(src);
}

static void test_role_user_assignment_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_role_user_assignment_parse(scalar));
    axiam_mgmt_role_user_assignment_free(NULL);
    cJSON_Delete(scalar);
}

/* `RotateSecretResponse`: a full wire object parses, builds back and frees. */
static void test_rotate_secret_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"client_secret\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_rotate_secret_response_t *model = axiam_mgmt_rotate_secret_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_rotate_secret_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_rotate_secret_response_free(model);
    cJSON_Delete(src);
}

/*
 * `RotateSecretResponse`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_rotate_secret_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_rotate_secret_response_t *model = axiam_mgmt_rotate_secret_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_rotate_secret_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_rotate_secret_response_free(model);
    cJSON_Delete(src);
}

static void test_rotate_secret_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_rotate_secret_response_parse(scalar));
    axiam_mgmt_rotate_secret_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `ScimTokenResponse`: a full wire object parses, builds back and frees. */
static void test_scim_token_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"created_by\": \"11111111-1111-4111-8111-111111111111\", \"expires_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"last_used_at\": \"2026-08-26T00:00:00Z\", \"name\": \"example\", \"revoked_at\": \"2026-08-26T00:00:00Z\", \"status\": \"active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_scim_token_response_t *model = axiam_mgmt_scim_token_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_scim_token_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_scim_token_response_free(model);
    cJSON_Delete(src);
}

/* `ScimTokenResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_scim_token_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"created_by\": \"11111111-1111-4111-8111-111111111111\", \"expires_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"status\": \"active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"user_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_scim_token_response_t *model = axiam_mgmt_scim_token_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_scim_token_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_scim_token_response_free(model);
    cJSON_Delete(src);
}

/*
 * `ScimTokenResponse`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_scim_token_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_scim_token_response_t *model = axiam_mgmt_scim_token_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_scim_token_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_scim_token_response_free(model);
    cJSON_Delete(src);
}

static void test_scim_token_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_scim_token_response_parse(scalar));
    axiam_mgmt_scim_token_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `Scope`: a full wire object parses, builds back and frees. */
static void test_scope_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"resource_id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_scope_t *model = axiam_mgmt_scope_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_scope_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_scope_free(model);
    cJSON_Delete(src);
}

/*
 * `Scope`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_scope_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_scope_t *model = axiam_mgmt_scope_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_scope_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_scope_free(model);
    cJSON_Delete(src);
}

static void test_scope_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_scope_parse(scalar));
    axiam_mgmt_scope_free(NULL);
    cJSON_Delete(scalar);
}

/* `SecuritySettings`: a full wire object parses, builds back and frees. */
static void test_security_settings_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"certificate\": {\"default_cert_validity_days\": 1, \"max_cert_validity_days\": 1}, \"created_at\": \"2026-08-26T00:00:00Z\", \"email\": {\"email_verification_grace_period_hours\": 1, \"email_verification_required\": true}, \"id\": \"11111111-1111-4111-8111-111111111111\", \"lockout\": {\"lockout_backoff_multiplier\": 1.5, \"lockout_duration_secs\": 1, \"max_failed_login_attempts\": 1, \"max_lockout_duration_secs\": 1}, \"mfa\": {\"mfa_challenge_lifetime_secs\": 1, \"mfa_enforced\": true}, \"notification\": {\"admin_notifications_enabled\": true}, \"opaque\": {\"opaque_ksf\": \"example\", \"opaque_mode\": \"example\", \"opaque_suite\": \"example\"}, \"password\": {\"hibp_check_enabled\": true, \"min_length\": 1, \"password_history_count\": 1, \"require_digits\": true, \"require_lowercase\": true, \"require_symbols\": true, \"require_uppercase\": true}, \"privacy\": {\"deletion_grace_period_days\": 1}, \"scope\": \"Org\", \"scope_id\": \"11111111-1111-4111-8111-111111111111\", \"token\": {\"access_token_lifetime_secs\": 1, \"refresh_token_lifetime_secs\": 1}, \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_security_settings_t *model = axiam_mgmt_security_settings_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_security_settings_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_security_settings_free(model);
    cJSON_Delete(src);
}

/*
 * `SecuritySettings`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_security_settings_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_security_settings_t *model = axiam_mgmt_security_settings_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_security_settings_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_security_settings_free(model);
    cJSON_Delete(src);
}

static void test_security_settings_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_security_settings_parse(scalar));
    axiam_mgmt_security_settings_free(NULL);
    cJSON_Delete(scalar);
}

/* `ServiceAccountCreatedResponse`: a full wire object parses, builds back and frees. */
static void test_service_account_created_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"client_id\": \"example\", \"client_secret\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_service_account_created_response_t *model = axiam_mgmt_service_account_created_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_service_account_created_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_service_account_created_response_free(model);
    cJSON_Delete(src);
}

/*
 * `ServiceAccountCreatedResponse`: the server omitting every OPTIONAL field is not an
 * error.
 */
static void test_service_account_created_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"client_id\": \"example\", \"client_secret\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_service_account_created_response_t *model = axiam_mgmt_service_account_created_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_service_account_created_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_service_account_created_response_free(model);
    cJSON_Delete(src);
}

/*
 * `ServiceAccountCreatedResponse`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_service_account_created_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_service_account_created_response_t *model = axiam_mgmt_service_account_created_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_service_account_created_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_service_account_created_response_free(model);
    cJSON_Delete(src);
}

static void test_service_account_created_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_service_account_created_response_parse(scalar));
    axiam_mgmt_service_account_created_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `ServiceAccountResponse`: a full wire object parses, builds back and frees. */
static void test_service_account_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"client_id\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"description\": \"example\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_service_account_response_t *model = axiam_mgmt_service_account_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_service_account_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_service_account_response_free(model);
    cJSON_Delete(src);
}

/* `ServiceAccountResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_service_account_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"client_id\": \"example\", \"created_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"name\": \"example\", \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_service_account_response_t *model = axiam_mgmt_service_account_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_service_account_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_service_account_response_free(model);
    cJSON_Delete(src);
}

/*
 * `ServiceAccountResponse`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_service_account_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_service_account_response_t *model = axiam_mgmt_service_account_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_service_account_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_service_account_response_free(model);
    cJSON_Delete(src);
}

static void test_service_account_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_service_account_response_parse(scalar));
    axiam_mgmt_service_account_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `SetMtlsTrustAnchor`: a full wire object parses, builds back and frees. */
static void test_set_mtls_trust_anchor_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"enabled\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_set_mtls_trust_anchor_t *model = axiam_mgmt_set_mtls_trust_anchor_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_set_mtls_trust_anchor_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_set_mtls_trust_anchor_free(model);
    cJSON_Delete(src);
}

/*
 * `SetMtlsTrustAnchor`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_set_mtls_trust_anchor_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_set_mtls_trust_anchor_t *model = axiam_mgmt_set_mtls_trust_anchor_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_set_mtls_trust_anchor_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_set_mtls_trust_anchor_free(model);
    cJSON_Delete(src);
}

static void test_set_mtls_trust_anchor_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_set_mtls_trust_anchor_parse(scalar));
    axiam_mgmt_set_mtls_trust_anchor_free(NULL);
    cJSON_Delete(scalar);
}

/* `SetOrgEmailConfig`: a full wire object parses, builds back and frees. */
static void test_set_org_email_config_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"enabled\": true, \"from_email\": \"example\", \"from_name\": \"example\", \"provider\": {}, \"reply_to\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_set_org_email_config_t *model = axiam_mgmt_set_org_email_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_set_org_email_config_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_set_org_email_config_free(model);
    cJSON_Delete(src);
}

/* `SetOrgEmailConfig`: the server omitting every OPTIONAL field is not an error. */
static void test_set_org_email_config_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"enabled\": true, \"from_email\": \"example\", \"from_name\": \"example\", \"provider\": {}}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_set_org_email_config_t *model = axiam_mgmt_set_org_email_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_set_org_email_config_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_set_org_email_config_free(model);
    cJSON_Delete(src);
}

/*
 * `SetOrgEmailConfig`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_set_org_email_config_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_set_org_email_config_t *model = axiam_mgmt_set_org_email_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_set_org_email_config_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_set_org_email_config_free(model);
    cJSON_Delete(src);
}

static void test_set_org_email_config_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_set_org_email_config_parse(scalar));
    axiam_mgmt_set_org_email_config_free(NULL);
    cJSON_Delete(scalar);
}

/* `SetOrgSettings`: a full wire object parses, builds back and frees. */
static void test_set_org_settings_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"access_token_lifetime_secs\": 1, \"admin_notifications_enabled\": true, \"default_cert_validity_days\": 1, \"deletion_grace_period_days\": 1, \"email_verification_grace_period_hours\": 1, \"email_verification_required\": true, \"hibp_check_enabled\": true, \"lockout_backoff_multiplier\": 1.5, \"lockout_duration_secs\": 1, \"max_cert_validity_days\": 1, \"max_failed_login_attempts\": 1, \"max_lockout_duration_secs\": 1, \"mfa_challenge_lifetime_secs\": 1, \"mfa_enforced\": true, \"min_length\": 1, \"opaque_ksf\": \"example\", \"opaque_mode\": \"example\", \"opaque_suite\": \"example\", \"password_history_count\": 1, \"refresh_token_lifetime_secs\": 1, \"require_digits\": true, \"require_lowercase\": true, \"require_symbols\": true, \"require_uppercase\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_set_org_settings_t *model = axiam_mgmt_set_org_settings_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_set_org_settings_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_set_org_settings_free(model);
    cJSON_Delete(src);
}

/* `SetOrgSettings`: the server omitting every OPTIONAL field is not an error. */
static void test_set_org_settings_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"access_token_lifetime_secs\": 1, \"admin_notifications_enabled\": true, \"default_cert_validity_days\": 1, \"email_verification_grace_period_hours\": 1, \"email_verification_required\": true, \"hibp_check_enabled\": true, \"lockout_backoff_multiplier\": 1.5, \"lockout_duration_secs\": 1, \"max_cert_validity_days\": 1, \"max_failed_login_attempts\": 1, \"max_lockout_duration_secs\": 1, \"mfa_challenge_lifetime_secs\": 1, \"mfa_enforced\": true, \"min_length\": 1, \"password_history_count\": 1, \"refresh_token_lifetime_secs\": 1, \"require_digits\": true, \"require_lowercase\": true, \"require_symbols\": true, \"require_uppercase\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_set_org_settings_t *model = axiam_mgmt_set_org_settings_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_set_org_settings_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_set_org_settings_free(model);
    cJSON_Delete(src);
}

/*
 * `SetOrgSettings`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_set_org_settings_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_set_org_settings_t *model = axiam_mgmt_set_org_settings_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_set_org_settings_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_set_org_settings_free(model);
    cJSON_Delete(src);
}

static void test_set_org_settings_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_set_org_settings_parse(scalar));
    axiam_mgmt_set_org_settings_free(NULL);
    cJSON_Delete(scalar);
}

/* `SignAuditBatchRequest`: a full wire object parses, builds back and frees. */
static void test_sign_audit_batch_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"entry_ids\": [\"11111111-1111-4111-8111-111111111111\"]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_sign_audit_batch_request_t *model = axiam_mgmt_sign_audit_batch_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_sign_audit_batch_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_sign_audit_batch_request_free(model);
    cJSON_Delete(src);
}

/*
 * `SignAuditBatchRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_sign_audit_batch_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_sign_audit_batch_request_t *model = axiam_mgmt_sign_audit_batch_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_sign_audit_batch_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_sign_audit_batch_request_free(model);
    cJSON_Delete(src);
}

static void test_sign_audit_batch_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_sign_audit_batch_request_parse(scalar));
    axiam_mgmt_sign_audit_batch_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `SignIntermediateCsrRequest`: a full wire object parses, builds back and frees. */
static void test_sign_intermediate_csr_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"csr_pem\": \"example\", \"parent_ca_id\": \"11111111-1111-4111-8111-111111111111\", \"validity_days\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_sign_intermediate_csr_request_t *model = axiam_mgmt_sign_intermediate_csr_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_sign_intermediate_csr_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_sign_intermediate_csr_request_free(model);
    cJSON_Delete(src);
}

/*
 * `SignIntermediateCsrRequest`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_sign_intermediate_csr_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_sign_intermediate_csr_request_t *model = axiam_mgmt_sign_intermediate_csr_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_sign_intermediate_csr_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_sign_intermediate_csr_request_free(model);
    cJSON_Delete(src);
}

static void test_sign_intermediate_csr_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_sign_intermediate_csr_request_parse(scalar));
    axiam_mgmt_sign_intermediate_csr_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `SignedAuditBatch`: a full wire object parses, builds back and frees. */
static void test_signed_audit_batch_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"batch_id\": \"11111111-1111-4111-8111-111111111111\", \"entry_ids\": [\"11111111-1111-4111-8111-111111111111\"], \"signature_armored\": \"example\", \"signed_at\": \"2026-08-26T00:00:00Z\", \"signing_key_id\": \"11111111-1111-4111-8111-111111111111\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_signed_audit_batch_t *model = axiam_mgmt_signed_audit_batch_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_signed_audit_batch_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_signed_audit_batch_free(model);
    cJSON_Delete(src);
}

/*
 * `SignedAuditBatch`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_signed_audit_batch_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_signed_audit_batch_t *model = axiam_mgmt_signed_audit_batch_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_signed_audit_batch_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_signed_audit_batch_free(model);
    cJSON_Delete(src);
}

static void test_signed_audit_batch_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_signed_audit_batch_parse(scalar));
    axiam_mgmt_signed_audit_batch_free(NULL);
    cJSON_Delete(scalar);
}

/* `SmtpConfig`: a full wire object parses, builds back and frees. */
static void test_smtp_config_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"host\": \"example\", \"port\": 1, \"starttls\": true, \"username\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_smtp_config_t *model = axiam_mgmt_smtp_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_smtp_config_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_smtp_config_free(model);
    cJSON_Delete(src);
}

/*
 * `SmtpConfig`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_smtp_config_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_smtp_config_t *model = axiam_mgmt_smtp_config_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_smtp_config_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_smtp_config_free(model);
    cJSON_Delete(src);
}

static void test_smtp_config_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_smtp_config_parse(scalar));
    axiam_mgmt_smtp_config_free(NULL);
    cJSON_Delete(scalar);
}

/* `Tenant`: a full wire object parses, builds back and frees. */
static void test_tenant_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"kind\": \"standard\", \"metadata\": {}, \"name\": \"example\", \"organization_id\": \"11111111-1111-4111-8111-111111111111\", \"slug\": \"example\", \"status\": \"Active\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_tenant_t *model = axiam_mgmt_tenant_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_tenant_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_tenant_free(model);
    cJSON_Delete(src);
}

/* `Tenant`: the server omitting every OPTIONAL field is not an error. */
static void test_tenant_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"id\": \"11111111-1111-4111-8111-111111111111\", \"metadata\": {}, \"name\": \"example\", \"organization_id\": \"11111111-1111-4111-8111-111111111111\", \"slug\": \"example\", \"status\": \"Active\", \"updated_at\": \"2026-08-26T00:00:00Z\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_tenant_t *model = axiam_mgmt_tenant_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_tenant_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_tenant_free(model);
    cJSON_Delete(src);
}

/*
 * `Tenant`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_tenant_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_tenant_t *model = axiam_mgmt_tenant_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_tenant_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_tenant_free(model);
    cJSON_Delete(src);
}

static void test_tenant_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_tenant_parse(scalar));
    axiam_mgmt_tenant_free(NULL);
    cJSON_Delete(scalar);
}

/* `TenantSettingsOverride`: a full wire object parses, builds back and frees. */
static void test_tenant_settings_override_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"access_token_lifetime_secs\": 1, \"admin_notifications_enabled\": true, \"default_cert_validity_days\": 1, \"deletion_grace_period_days\": 1, \"email_verification_grace_period_hours\": 1, \"email_verification_required\": true, \"hibp_check_enabled\": true, \"lockout_backoff_multiplier\": 1.5, \"lockout_duration_secs\": 1, \"max_cert_validity_days\": 1, \"max_failed_login_attempts\": 1, \"max_lockout_duration_secs\": 1, \"mfa_challenge_lifetime_secs\": 1, \"mfa_enforced\": true, \"min_length\": 1, \"opaque_ksf\": \"example\", \"opaque_mode\": \"example\", \"opaque_suite\": \"example\", \"password_history_count\": 1, \"refresh_token_lifetime_secs\": 1, \"require_digits\": true, \"require_lowercase\": true, \"require_symbols\": true, \"require_uppercase\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_tenant_settings_override_t *model = axiam_mgmt_tenant_settings_override_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_tenant_settings_override_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_tenant_settings_override_free(model);
    cJSON_Delete(src);
}

/* `TenantSettingsOverride`: the server omitting every OPTIONAL field is not an error. */
static void test_tenant_settings_override_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_tenant_settings_override_t *model = axiam_mgmt_tenant_settings_override_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_tenant_settings_override_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_tenant_settings_override_free(model);
    cJSON_Delete(src);
}

/*
 * `TenantSettingsOverride`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_tenant_settings_override_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_tenant_settings_override_t *model = axiam_mgmt_tenant_settings_override_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_tenant_settings_override_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_tenant_settings_override_free(model);
    cJSON_Delete(src);
}

static void test_tenant_settings_override_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_tenant_settings_override_parse(scalar));
    axiam_mgmt_tenant_settings_override_free(NULL);
    cJSON_Delete(scalar);
}

/* `TokenExchangeTrustRequest`: a full wire object parses, builds back and frees. */
static void test_token_exchange_trust_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"accepted_audiences\": [\"example\"], \"enabled\": true, \"max_lifetime_secs\": 1, \"max_token_age_secs\": 1, \"scope_map\": {}, \"subject_mapping\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_token_exchange_trust_request_t *model = axiam_mgmt_token_exchange_trust_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_token_exchange_trust_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_token_exchange_trust_request_free(model);
    cJSON_Delete(src);
}

/* `TokenExchangeTrustRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_token_exchange_trust_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_token_exchange_trust_request_t *model = axiam_mgmt_token_exchange_trust_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_token_exchange_trust_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_token_exchange_trust_request_free(model);
    cJSON_Delete(src);
}

/*
 * `TokenExchangeTrustRequest`: an EMPTY object parses without crashing. A server that omits
 * a field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_token_exchange_trust_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_token_exchange_trust_request_t *model = axiam_mgmt_token_exchange_trust_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_token_exchange_trust_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_token_exchange_trust_request_free(model);
    cJSON_Delete(src);
}

static void test_token_exchange_trust_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_token_exchange_trust_request_parse(scalar));
    axiam_mgmt_token_exchange_trust_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `TokenExchangeTrustResponse`: a full wire object parses, builds back and frees. */
static void test_token_exchange_trust_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"accepted_audiences\": [\"example\"], \"enabled\": true, \"max_lifetime_secs\": 1, \"max_token_age_secs\": 1, \"scope_map\": {}, \"subject_mapping\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_token_exchange_trust_response_t *model = axiam_mgmt_token_exchange_trust_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_token_exchange_trust_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_token_exchange_trust_response_free(model);
    cJSON_Delete(src);
}

/* `TokenExchangeTrustResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_token_exchange_trust_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"accepted_audiences\": [\"example\"], \"enabled\": true, \"max_token_age_secs\": 1, \"scope_map\": {}, \"subject_mapping\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_token_exchange_trust_response_t *model = axiam_mgmt_token_exchange_trust_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_token_exchange_trust_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_token_exchange_trust_response_free(model);
    cJSON_Delete(src);
}

/*
 * `TokenExchangeTrustResponse`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_token_exchange_trust_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_token_exchange_trust_response_t *model = axiam_mgmt_token_exchange_trust_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_token_exchange_trust_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_token_exchange_trust_response_free(model);
    cJSON_Delete(src);
}

static void test_token_exchange_trust_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_token_exchange_trust_response_parse(scalar));
    axiam_mgmt_token_exchange_trust_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `TokenPolicy`: a full wire object parses, builds back and frees. */
static void test_token_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"access_token_lifetime_secs\": 1, \"refresh_token_lifetime_secs\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_token_policy_t *model = axiam_mgmt_token_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_token_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_token_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `TokenPolicy`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_token_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_token_policy_t *model = axiam_mgmt_token_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_token_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_token_policy_free(model);
    cJSON_Delete(src);
}

static void test_token_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_token_policy_parse(scalar));
    axiam_mgmt_token_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateFederationConfigRequest`: a full wire object parses, builds back and frees. */
static void test_update_federation_config_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"allowed_algorithms\": [\"example\"], \"attribute_map\": {}, \"client_id\": \"example\", \"client_secret\": \"example\", \"enabled\": true, \"idp_signing_cert_pem\": \"example\", \"metadata_url\": \"example\", \"provider\": \"example\", \"token_exchange\": {\"accepted_audiences\": [\"example\"], \"enabled\": true, \"max_lifetime_secs\": 1, \"max_token_age_secs\": 1, \"scope_map\": {}, \"subject_mapping\": \"example\"}}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_federation_config_request_t *model = axiam_mgmt_update_federation_config_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_federation_config_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_federation_config_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateFederationConfigRequest`: the server omitting every OPTIONAL field is not an
 * error.
 */
static void test_update_federation_config_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_federation_config_request_t *model = axiam_mgmt_update_federation_config_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_federation_config_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_federation_config_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateFederationConfigRequest`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_update_federation_config_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_federation_config_request_t *model = axiam_mgmt_update_federation_config_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_federation_config_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_federation_config_request_free(model);
    cJSON_Delete(src);
}

static void test_update_federation_config_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_federation_config_request_parse(scalar));
    axiam_mgmt_update_federation_config_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateGroup`: a full wire object parses, builds back and frees. */
static void test_update_group_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"metadata\": {}, \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_group_t *model = axiam_mgmt_update_group_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_group_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_group_free(model);
    cJSON_Delete(src);
}

/* `UpdateGroup`: the server omitting every OPTIONAL field is not an error. */
static void test_update_group_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_group_t *model = axiam_mgmt_update_group_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_group_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_group_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateGroup`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_update_group_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_group_t *model = axiam_mgmt_update_group_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_group_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_group_free(model);
    cJSON_Delete(src);
}

static void test_update_group_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_group_parse(scalar));
    axiam_mgmt_update_group_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateNotificationRuleRequest`: a full wire object parses, builds back and frees. */
static void test_update_notification_rule_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"enabled\": true, \"events\": [\"login_failure\"], \"name\": \"example\", \"recipient_emails\": [\"example\"]}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_notification_rule_request_t *model = axiam_mgmt_update_notification_rule_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_notification_rule_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_notification_rule_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateNotificationRuleRequest`: the server omitting every OPTIONAL field is not an
 * error.
 */
static void test_update_notification_rule_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_notification_rule_request_t *model = axiam_mgmt_update_notification_rule_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_notification_rule_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_notification_rule_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateNotificationRuleRequest`: an EMPTY object parses without crashing. A server that
 * omits a field openapi.json marks required is malformed, and this SDK's answer is a model
 * with that member unset rather than an abort -- the caller is in a position to decide, and
 * a parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_update_notification_rule_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_notification_rule_request_t *model = axiam_mgmt_update_notification_rule_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_notification_rule_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_notification_rule_request_free(model);
    cJSON_Delete(src);
}

static void test_update_notification_rule_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_notification_rule_request_parse(scalar));
    axiam_mgmt_update_notification_rule_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateOAuth2ClientRequest`: a full wire object parses, builds back and frees. */
static void test_update_o_auth2_client_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"backchannel_logout_uri\": \"example\", \"dpop_bound_access_tokens\": true, \"dpop_require_nonce\": true, \"grant_types\": [\"example\"], \"jwks\": \"example\", \"jwks_uri\": \"example\", \"name\": \"example\", \"post_logout_redirect_uris\": [\"example\"], \"profile\": \"standard\", \"redirect_uris\": [\"example\"], \"require_par\": true, \"scopes\": [\"example\"], \"self_signed_tls_client_auth_thumbprints\": [\"example\"], \"tls_client_auth_san_dns\": \"example\", \"tls_client_auth_san_uri\": \"example\", \"tls_client_auth_subject_dn\": \"example\", \"tls_client_certificate_bound_access_tokens\": true, \"token_endpoint_auth_method\": \"client_secret_post\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_o_auth2_client_request_t *model = axiam_mgmt_update_o_auth2_client_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_o_auth2_client_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_o_auth2_client_request_free(model);
    cJSON_Delete(src);
}

/* `UpdateOAuth2ClientRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_update_o_auth2_client_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_o_auth2_client_request_t *model = axiam_mgmt_update_o_auth2_client_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_o_auth2_client_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_o_auth2_client_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateOAuth2ClientRequest`: an EMPTY object parses without crashing. A server that omits
 * a field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_update_o_auth2_client_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_o_auth2_client_request_t *model = axiam_mgmt_update_o_auth2_client_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_o_auth2_client_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_o_auth2_client_request_free(model);
    cJSON_Delete(src);
}

static void test_update_o_auth2_client_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_o_auth2_client_request_parse(scalar));
    axiam_mgmt_update_o_auth2_client_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateOrganizationRequest`: a full wire object parses, builds back and frees. */
static void test_update_organization_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"metadata\": {}, \"name\": \"example\", \"slug\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_organization_request_t *model = axiam_mgmt_update_organization_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_organization_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_organization_request_free(model);
    cJSON_Delete(src);
}

/* `UpdateOrganizationRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_update_organization_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_organization_request_t *model = axiam_mgmt_update_organization_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_organization_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_organization_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateOrganizationRequest`: an EMPTY object parses without crashing. A server that omits
 * a field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_update_organization_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_organization_request_t *model = axiam_mgmt_update_organization_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_organization_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_organization_request_free(model);
    cJSON_Delete(src);
}

static void test_update_organization_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_organization_request_parse(scalar));
    axiam_mgmt_update_organization_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdatePermissionRequest`: a full wire object parses, builds back and frees. */
static void test_update_permission_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"action\": \"example\", \"description\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_permission_request_t *model = axiam_mgmt_update_permission_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_permission_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_permission_request_free(model);
    cJSON_Delete(src);
}

/* `UpdatePermissionRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_update_permission_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_permission_request_t *model = axiam_mgmt_update_permission_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_permission_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_permission_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdatePermissionRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_update_permission_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_permission_request_t *model = axiam_mgmt_update_permission_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_permission_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_permission_request_free(model);
    cJSON_Delete(src);
}

static void test_update_permission_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_permission_request_parse(scalar));
    axiam_mgmt_update_permission_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateReactorRequest`: a full wire object parses, builds back and frees. */
static void test_update_reactor_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"enabled\": true, \"events\": [\"example\"], \"failure_policy\": \"fail_closed\", \"mode\": \"intercept\", \"name\": \"example\", \"priority\": 1, \"timeout_ms\": 1}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_reactor_request_t *model = axiam_mgmt_update_reactor_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_reactor_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_reactor_request_free(model);
    cJSON_Delete(src);
}

/* `UpdateReactorRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_update_reactor_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_reactor_request_t *model = axiam_mgmt_update_reactor_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_reactor_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_reactor_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateReactorRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_update_reactor_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_reactor_request_t *model = axiam_mgmt_update_reactor_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_reactor_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_reactor_request_free(model);
    cJSON_Delete(src);
}

static void test_update_reactor_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_reactor_request_parse(scalar));
    axiam_mgmt_update_reactor_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateResourceRequest`: a full wire object parses, builds back and frees. */
static void test_update_resource_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"metadata\": {}, \"name\": \"example\", \"parent_id\": \"11111111-1111-4111-8111-111111111111\", \"resource_type\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_resource_request_t *model = axiam_mgmt_update_resource_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_resource_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_resource_request_free(model);
    cJSON_Delete(src);
}

/* `UpdateResourceRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_update_resource_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_resource_request_t *model = axiam_mgmt_update_resource_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_resource_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_resource_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateResourceRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_update_resource_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_resource_request_t *model = axiam_mgmt_update_resource_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_resource_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_resource_request_free(model);
    cJSON_Delete(src);
}

static void test_update_resource_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_resource_request_parse(scalar));
    axiam_mgmt_update_resource_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateRole`: a full wire object parses, builds back and frees. */
static void test_update_role_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"is_global\": true, \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_role_t *model = axiam_mgmt_update_role_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_role_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_role_free(model);
    cJSON_Delete(src);
}

/* `UpdateRole`: the server omitting every OPTIONAL field is not an error. */
static void test_update_role_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_role_t *model = axiam_mgmt_update_role_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_role_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_role_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateRole`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_update_role_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_role_t *model = axiam_mgmt_update_role_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_role_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_role_free(model);
    cJSON_Delete(src);
}

static void test_update_role_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_role_parse(scalar));
    axiam_mgmt_update_role_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateScopeRequest`: a full wire object parses, builds back and frees. */
static void test_update_scope_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"name\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_scope_request_t *model = axiam_mgmt_update_scope_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_scope_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_scope_request_free(model);
    cJSON_Delete(src);
}

/* `UpdateScopeRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_update_scope_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_scope_request_t *model = axiam_mgmt_update_scope_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_scope_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_scope_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateScopeRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_update_scope_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_scope_request_t *model = axiam_mgmt_update_scope_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_scope_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_scope_request_free(model);
    cJSON_Delete(src);
}

static void test_update_scope_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_scope_request_parse(scalar));
    axiam_mgmt_update_scope_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateServiceAccount`: a full wire object parses, builds back and frees. */
static void test_update_service_account_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"description\": \"example\", \"name\": \"example\", \"status\": \"Active\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_service_account_t *model = axiam_mgmt_update_service_account_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_service_account_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_service_account_free(model);
    cJSON_Delete(src);
}

/* `UpdateServiceAccount`: the server omitting every OPTIONAL field is not an error. */
static void test_update_service_account_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_service_account_t *model = axiam_mgmt_update_service_account_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_service_account_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_service_account_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateServiceAccount`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_update_service_account_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_service_account_t *model = axiam_mgmt_update_service_account_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_service_account_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_service_account_free(model);
    cJSON_Delete(src);
}

static void test_update_service_account_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_service_account_parse(scalar));
    axiam_mgmt_update_service_account_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateTenant`: a full wire object parses, builds back and frees. */
static void test_update_tenant_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"metadata\": {}, \"name\": \"example\", \"slug\": \"example\", \"status\": \"Active\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_tenant_t *model = axiam_mgmt_update_tenant_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_tenant_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_tenant_free(model);
    cJSON_Delete(src);
}

/* `UpdateTenant`: the server omitting every OPTIONAL field is not an error. */
static void test_update_tenant_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_tenant_t *model = axiam_mgmt_update_tenant_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_tenant_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_tenant_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateTenant`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_update_tenant_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_tenant_t *model = axiam_mgmt_update_tenant_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_tenant_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_tenant_free(model);
    cJSON_Delete(src);
}

static void test_update_tenant_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_tenant_parse(scalar));
    axiam_mgmt_update_tenant_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateUserRequest`: a full wire object parses, builds back and frees. */
static void test_update_user_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"email\": \"example\", \"metadata\": {}, \"status\": \"Active\", \"username\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_user_request_t *model = axiam_mgmt_update_user_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_user_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_user_request_free(model);
    cJSON_Delete(src);
}

/* `UpdateUserRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_update_user_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_user_request_t *model = axiam_mgmt_update_user_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_user_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_user_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateUserRequest`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_update_user_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_user_request_t *model = axiam_mgmt_update_user_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_user_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_user_request_free(model);
    cJSON_Delete(src);
}

static void test_update_user_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_user_request_parse(scalar));
    axiam_mgmt_update_user_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `UpdateWebhookRequest`: a full wire object parses, builds back and frees. */
static void test_update_webhook_request_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"enabled\": true, \"events\": [\"example\"], \"retry_policy\": {\"backoff_multiplier\": 1.5, \"initial_delay_secs\": 1, \"max_retries\": 1}, \"secret\": \"example\", \"url\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_webhook_request_t *model = axiam_mgmt_update_webhook_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_webhook_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_webhook_request_free(model);
    cJSON_Delete(src);
}

/* `UpdateWebhookRequest`: the server omitting every OPTIONAL field is not an error. */
static void test_update_webhook_request_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_webhook_request_t *model = axiam_mgmt_update_webhook_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_update_webhook_request_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_update_webhook_request_free(model);
    cJSON_Delete(src);
}

/*
 * `UpdateWebhookRequest`: an EMPTY object parses without crashing. A server that omits a
 * field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_update_webhook_request_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_update_webhook_request_t *model = axiam_mgmt_update_webhook_request_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_update_webhook_request_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_update_webhook_request_free(model);
    cJSON_Delete(src);
}

static void test_update_webhook_request_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_update_webhook_request_parse(scalar));
    axiam_mgmt_update_webhook_request_free(NULL);
    cJSON_Delete(scalar);
}

/* `UserResponse`: a full wire object parses, builds back and frees. */
static void test_user_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"email\": \"example\", \"email_verified\": true, \"failed_login_attempts\": 1, \"id\": \"11111111-1111-4111-8111-111111111111\", \"is_locked\": true, \"locked_until\": \"2026-08-26T00:00:00Z\", \"metadata\": {}, \"mfa_enabled\": true, \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\", \"username\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_user_response_t *model = axiam_mgmt_user_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_user_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_user_response_free(model);
    cJSON_Delete(src);
}

/* `UserResponse`: the server omitting every OPTIONAL field is not an error. */
static void test_user_response_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"email\": \"example\", \"email_verified\": true, \"failed_login_attempts\": 1, \"id\": \"11111111-1111-4111-8111-111111111111\", \"is_locked\": true, \"metadata\": {}, \"mfa_enabled\": true, \"status\": \"Active\", \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\", \"username\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_user_response_t *model = axiam_mgmt_user_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_user_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_user_response_free(model);
    cJSON_Delete(src);
}

/*
 * `UserResponse`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_user_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_user_response_t *model = axiam_mgmt_user_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_user_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_user_response_free(model);
    cJSON_Delete(src);
}

static void test_user_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_user_response_parse(scalar));
    axiam_mgmt_user_response_free(NULL);
    cJSON_Delete(scalar);
}

/* `WebauthnAttestationPolicy`: a full wire object parses, builds back and frees. */
static void test_webauthn_attestation_policy_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"allowed_aaguids\": [\"11111111-1111-4111-8111-111111111111\"], \"block_revoked_status\": true, \"blocked_aaguids\": [\"11111111-1111-4111-8111-111111111111\"], \"min_certification\": \"L1\", \"mode\": \"none\", \"require_fido_certified\": true, \"unknown_aaguid\": \"allow\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_webauthn_attestation_policy_t *model = axiam_mgmt_webauthn_attestation_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_webauthn_attestation_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_webauthn_attestation_policy_free(model);
    cJSON_Delete(src);
}

/* `WebauthnAttestationPolicy`: the server omitting every OPTIONAL field is not an error. */
static void test_webauthn_attestation_policy_parses_without_optionals(void) {
    cJSON *src = cJSON_Parse("{\"block_revoked_status\": true, \"mode\": \"none\", \"require_fido_certified\": true}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_webauthn_attestation_policy_t *model = axiam_mgmt_webauthn_attestation_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    /*
     * Building it back must not invent the fields that were absent: an unset member is
     * OMITTED, not emitted as null (27.4 rule 5).
     */
    cJSON *rebuilt = axiam_mgmt_webauthn_attestation_policy_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    for (const cJSON *f = rebuilt->child; f; f = f->next)
        TEST_ASSERT_FALSE_MESSAGE(cJSON_IsNull(f), f->string);

    cJSON_Delete(rebuilt);
    axiam_mgmt_webauthn_attestation_policy_free(model);
    cJSON_Delete(src);
}

/*
 * `WebauthnAttestationPolicy`: an EMPTY object parses without crashing. A server that omits
 * a field openapi.json marks required is malformed, and this SDK's answer is a model with
 * that member unset rather than an abort -- the caller is in a position to decide, and a
 * parser that segfaults on a bad response is a worse failure than one that hands back a
 * null. This also reaches the ABSENT arm of every field guard, required ones included,
 * which no well-formed fixture can.
 */
static void test_webauthn_attestation_policy_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_webauthn_attestation_policy_t *model = axiam_mgmt_webauthn_attestation_policy_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_webauthn_attestation_policy_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_webauthn_attestation_policy_free(model);
    cJSON_Delete(src);
}

static void test_webauthn_attestation_policy_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_webauthn_attestation_policy_parse(scalar));
    axiam_mgmt_webauthn_attestation_policy_free(NULL);
    cJSON_Delete(scalar);
}

/* `WebhookResponse`: a full wire object parses, builds back and frees. */
static void test_webhook_response_round_trips(void) {
    cJSON *src = cJSON_Parse("{\"created_at\": \"2026-08-26T00:00:00Z\", \"enabled\": true, \"events\": [\"example\"], \"id\": \"11111111-1111-4111-8111-111111111111\", \"retry_policy\": {\"backoff_multiplier\": 1.5, \"initial_delay_secs\": 1, \"max_retries\": 1}, \"tenant_id\": \"11111111-1111-4111-8111-111111111111\", \"updated_at\": \"2026-08-26T00:00:00Z\", \"url\": \"example\"}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_webhook_response_t *model = axiam_mgmt_webhook_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_webhook_response_build(model);
    TEST_ASSERT_NOT_NULL(rebuilt);
    /*
     * Every key the server sent must survive parse AND build. A field the model can read
     * but not write again is data it silently loses on anything the SDK re-sends.
     */
    for (const cJSON *f = src->child; f; f = f->next) {
        if (cJSON_IsNull(f)) continue;
        TEST_ASSERT_NOT_NULL_MESSAGE(
            cJSON_GetObjectItemCaseSensitive(rebuilt, f->string), f->string);
    }

    cJSON_Delete(rebuilt);
    axiam_mgmt_webhook_response_free(model);
    cJSON_Delete(src);
}

/*
 * `WebhookResponse`: an EMPTY object parses without crashing. A server that omits a field
 * openapi.json marks required is malformed, and this SDK's answer is a model with that
 * member unset rather than an abort -- the caller is in a position to decide, and a parser
 * that segfaults on a bad response is a worse failure than one that hands back a null. This
 * also reaches the ABSENT arm of every field guard, required ones included, which no
 * well-formed fixture can.
 */
static void test_webhook_response_parses_an_empty_object(void) {
    cJSON *src = cJSON_Parse("{}");
    TEST_ASSERT_NOT_NULL(src);

    axiam_mgmt_webhook_response_t *model = axiam_mgmt_webhook_response_parse(src);
    TEST_ASSERT_NOT_NULL(model);

    cJSON *rebuilt = axiam_mgmt_webhook_response_build(model);
    if (rebuilt) cJSON_Delete(rebuilt);
    axiam_mgmt_webhook_response_free(model);
    cJSON_Delete(src);
}

static void test_webhook_response_rejects_a_non_object(void) {
    cJSON *scalar = cJSON_CreateString("nope");
    TEST_ASSERT_NULL(axiam_mgmt_webhook_response_parse(scalar));
    axiam_mgmt_webhook_response_free(NULL);
    cJSON_Delete(scalar);
}

int main(void) {
    UNITY_BEGIN();
    RUN_TEST(test_add_member_request_round_trips);
    RUN_TEST(test_add_member_request_parses_an_empty_object);
    RUN_TEST(test_add_member_request_rejects_a_non_object);
    RUN_TEST(test_add_service_account_member_request_round_trips);
    RUN_TEST(test_add_service_account_member_request_parses_an_empty_object);
    RUN_TEST(test_add_service_account_member_request_rejects_a_non_object);
    RUN_TEST(test_api_provider_config_round_trips);
    RUN_TEST(test_api_provider_config_parses_without_optionals);
    RUN_TEST(test_api_provider_config_parses_an_empty_object);
    RUN_TEST(test_api_provider_config_rejects_a_non_object);
    RUN_TEST(test_assign_role_to_group_request_round_trips);
    RUN_TEST(test_assign_role_to_group_request_parses_without_optionals);
    RUN_TEST(test_assign_role_to_group_request_parses_an_empty_object);
    RUN_TEST(test_assign_role_to_group_request_rejects_a_non_object);
    RUN_TEST(test_assign_role_to_service_account_request_round_trips);
    RUN_TEST(test_assign_role_to_service_account_request_parses_without_optionals);
    RUN_TEST(test_assign_role_to_service_account_request_parses_an_empty_object);
    RUN_TEST(test_assign_role_to_service_account_request_rejects_a_non_object);
    RUN_TEST(test_assign_role_to_user_request_round_trips);
    RUN_TEST(test_assign_role_to_user_request_parses_without_optionals);
    RUN_TEST(test_assign_role_to_user_request_parses_an_empty_object);
    RUN_TEST(test_assign_role_to_user_request_rejects_a_non_object);
    RUN_TEST(test_audit_log_entry_round_trips);
    RUN_TEST(test_audit_log_entry_parses_without_optionals);
    RUN_TEST(test_audit_log_entry_parses_an_empty_object);
    RUN_TEST(test_audit_log_entry_rejects_a_non_object);
    RUN_TEST(test_bind_certificate_round_trips);
    RUN_TEST(test_bind_certificate_parses_an_empty_object);
    RUN_TEST(test_bind_certificate_rejects_a_non_object);
    RUN_TEST(test_ca_certificate_round_trips);
    RUN_TEST(test_ca_certificate_parses_without_optionals);
    RUN_TEST(test_ca_certificate_parses_an_empty_object);
    RUN_TEST(test_ca_certificate_rejects_a_non_object);
    RUN_TEST(test_certificate_round_trips);
    RUN_TEST(test_certificate_parses_an_empty_object);
    RUN_TEST(test_certificate_rejects_a_non_object);
    RUN_TEST(test_certificate_policy_round_trips);
    RUN_TEST(test_certificate_policy_parses_an_empty_object);
    RUN_TEST(test_certificate_policy_rejects_a_non_object);
    RUN_TEST(test_compliance_report_entry_round_trips);
    RUN_TEST(test_compliance_report_entry_parses_without_optionals);
    RUN_TEST(test_compliance_report_entry_parses_an_empty_object);
    RUN_TEST(test_compliance_report_entry_rejects_a_non_object);
    RUN_TEST(test_create_ca_certificate_request_round_trips);
    RUN_TEST(test_create_ca_certificate_request_parses_without_optionals);
    RUN_TEST(test_create_ca_certificate_request_parses_an_empty_object);
    RUN_TEST(test_create_ca_certificate_request_rejects_a_non_object);
    RUN_TEST(test_create_certificate_request_round_trips);
    RUN_TEST(test_create_certificate_request_parses_without_optionals);
    RUN_TEST(test_create_certificate_request_parses_an_empty_object);
    RUN_TEST(test_create_certificate_request_rejects_a_non_object);
    RUN_TEST(test_create_federation_config_request_round_trips);
    RUN_TEST(test_create_federation_config_request_parses_without_optionals);
    RUN_TEST(test_create_federation_config_request_parses_an_empty_object);
    RUN_TEST(test_create_federation_config_request_rejects_a_non_object);
    RUN_TEST(test_create_group_request_round_trips);
    RUN_TEST(test_create_group_request_parses_without_optionals);
    RUN_TEST(test_create_group_request_parses_an_empty_object);
    RUN_TEST(test_create_group_request_rejects_a_non_object);
    RUN_TEST(test_create_intermediate_ca_request_round_trips);
    RUN_TEST(test_create_intermediate_ca_request_parses_an_empty_object);
    RUN_TEST(test_create_intermediate_ca_request_rejects_a_non_object);
    RUN_TEST(test_create_notification_rule_request_round_trips);
    RUN_TEST(test_create_notification_rule_request_parses_an_empty_object);
    RUN_TEST(test_create_notification_rule_request_rejects_a_non_object);
    RUN_TEST(test_create_o_auth2_client_request_round_trips);
    RUN_TEST(test_create_o_auth2_client_request_parses_without_optionals);
    RUN_TEST(test_create_o_auth2_client_request_parses_an_empty_object);
    RUN_TEST(test_create_o_auth2_client_request_rejects_a_non_object);
    RUN_TEST(test_create_permission_request_round_trips);
    RUN_TEST(test_create_permission_request_parses_an_empty_object);
    RUN_TEST(test_create_permission_request_rejects_a_non_object);
    RUN_TEST(test_create_pgp_key_request_round_trips);
    RUN_TEST(test_create_pgp_key_request_parses_an_empty_object);
    RUN_TEST(test_create_pgp_key_request_rejects_a_non_object);
    RUN_TEST(test_create_reactor_request_round_trips);
    RUN_TEST(test_create_reactor_request_parses_without_optionals);
    RUN_TEST(test_create_reactor_request_parses_an_empty_object);
    RUN_TEST(test_create_reactor_request_rejects_a_non_object);
    RUN_TEST(test_create_resource_request_round_trips);
    RUN_TEST(test_create_resource_request_parses_without_optionals);
    RUN_TEST(test_create_resource_request_parses_an_empty_object);
    RUN_TEST(test_create_resource_request_rejects_a_non_object);
    RUN_TEST(test_create_role_request_round_trips);
    RUN_TEST(test_create_role_request_parses_an_empty_object);
    RUN_TEST(test_create_role_request_rejects_a_non_object);
    RUN_TEST(test_create_scim_token_request_round_trips);
    RUN_TEST(test_create_scim_token_request_parses_without_optionals);
    RUN_TEST(test_create_scim_token_request_parses_an_empty_object);
    RUN_TEST(test_create_scim_token_request_rejects_a_non_object);
    RUN_TEST(test_create_scim_token_response_round_trips);
    RUN_TEST(test_create_scim_token_response_parses_without_optionals);
    RUN_TEST(test_create_scim_token_response_parses_an_empty_object);
    RUN_TEST(test_create_scim_token_response_rejects_a_non_object);
    RUN_TEST(test_create_scope_request_round_trips);
    RUN_TEST(test_create_scope_request_parses_an_empty_object);
    RUN_TEST(test_create_scope_request_rejects_a_non_object);
    RUN_TEST(test_create_service_account_request_round_trips);
    RUN_TEST(test_create_service_account_request_parses_without_optionals);
    RUN_TEST(test_create_service_account_request_parses_an_empty_object);
    RUN_TEST(test_create_service_account_request_rejects_a_non_object);
    RUN_TEST(test_create_tenant_request_round_trips);
    RUN_TEST(test_create_tenant_request_parses_without_optionals);
    RUN_TEST(test_create_tenant_request_parses_an_empty_object);
    RUN_TEST(test_create_tenant_request_rejects_a_non_object);
    RUN_TEST(test_create_user_request_round_trips);
    RUN_TEST(test_create_user_request_parses_without_optionals);
    RUN_TEST(test_create_user_request_parses_an_empty_object);
    RUN_TEST(test_create_user_request_rejects_a_non_object);
    RUN_TEST(test_create_webhook_request_round_trips);
    RUN_TEST(test_create_webhook_request_parses_without_optionals);
    RUN_TEST(test_create_webhook_request_parses_an_empty_object);
    RUN_TEST(test_create_webhook_request_rejects_a_non_object);
    RUN_TEST(test_email_config_round_trips);
    RUN_TEST(test_email_config_parses_without_optionals);
    RUN_TEST(test_email_config_parses_an_empty_object);
    RUN_TEST(test_email_config_rejects_a_non_object);
    RUN_TEST(test_email_config_override_round_trips);
    RUN_TEST(test_email_config_override_parses_without_optionals);
    RUN_TEST(test_email_config_override_parses_an_empty_object);
    RUN_TEST(test_email_config_override_rejects_a_non_object);
    RUN_TEST(test_email_test_result_round_trips);
    RUN_TEST(test_email_test_result_parses_without_optionals);
    RUN_TEST(test_email_test_result_parses_an_empty_object);
    RUN_TEST(test_email_test_result_rejects_a_non_object);
    RUN_TEST(test_email_verification_policy_round_trips);
    RUN_TEST(test_email_verification_policy_parses_an_empty_object);
    RUN_TEST(test_email_verification_policy_rejects_a_non_object);
    RUN_TEST(test_encrypt_request_round_trips);
    RUN_TEST(test_encrypt_request_parses_an_empty_object);
    RUN_TEST(test_encrypt_request_rejects_a_non_object);
    RUN_TEST(test_encrypted_export_round_trips);
    RUN_TEST(test_encrypted_export_parses_an_empty_object);
    RUN_TEST(test_encrypted_export_rejects_a_non_object);
    RUN_TEST(test_federation_config_response_round_trips);
    RUN_TEST(test_federation_config_response_parses_without_optionals);
    RUN_TEST(test_federation_config_response_parses_an_empty_object);
    RUN_TEST(test_federation_config_response_rejects_a_non_object);
    RUN_TEST(test_federation_link_response_round_trips);
    RUN_TEST(test_federation_link_response_parses_without_optionals);
    RUN_TEST(test_federation_link_response_parses_an_empty_object);
    RUN_TEST(test_federation_link_response_rejects_a_non_object);
    RUN_TEST(test_generated_ca_certificate_round_trips);
    RUN_TEST(test_generated_ca_certificate_parses_without_optionals);
    RUN_TEST(test_generated_ca_certificate_parses_an_empty_object);
    RUN_TEST(test_generated_ca_certificate_rejects_a_non_object);
    RUN_TEST(test_generated_certificate_round_trips);
    RUN_TEST(test_generated_certificate_parses_without_optionals);
    RUN_TEST(test_generated_certificate_parses_an_empty_object);
    RUN_TEST(test_generated_certificate_rejects_a_non_object);
    RUN_TEST(test_generated_pgp_key_round_trips);
    RUN_TEST(test_generated_pgp_key_parses_without_optionals);
    RUN_TEST(test_generated_pgp_key_parses_an_empty_object);
    RUN_TEST(test_generated_pgp_key_rejects_a_non_object);
    RUN_TEST(test_grant_permission_request_round_trips);
    RUN_TEST(test_grant_permission_request_parses_without_optionals);
    RUN_TEST(test_grant_permission_request_parses_an_empty_object);
    RUN_TEST(test_grant_permission_request_rejects_a_non_object);
    RUN_TEST(test_granted_scope_round_trips);
    RUN_TEST(test_granted_scope_parses_an_empty_object);
    RUN_TEST(test_granted_scope_rejects_a_non_object);
    RUN_TEST(test_group_round_trips);
    RUN_TEST(test_group_parses_an_empty_object);
    RUN_TEST(test_group_rejects_a_non_object);
    RUN_TEST(test_health_response_round_trips);
    RUN_TEST(test_health_response_parses_an_empty_object);
    RUN_TEST(test_health_response_rejects_a_non_object);
    RUN_TEST(test_import_ca_certificate_request_round_trips);
    RUN_TEST(test_import_ca_certificate_request_parses_without_optionals);
    RUN_TEST(test_import_ca_certificate_request_parses_an_empty_object);
    RUN_TEST(test_import_ca_certificate_request_rejects_a_non_object);
    RUN_TEST(test_lockout_policy_round_trips);
    RUN_TEST(test_lockout_policy_parses_an_empty_object);
    RUN_TEST(test_lockout_policy_rejects_a_non_object);
    RUN_TEST(test_mds_status_response_round_trips);
    RUN_TEST(test_mds_status_response_parses_without_optionals);
    RUN_TEST(test_mds_status_response_parses_an_empty_object);
    RUN_TEST(test_mds_status_response_rejects_a_non_object);
    RUN_TEST(test_mfa_method_response_round_trips);
    RUN_TEST(test_mfa_method_response_parses_without_optionals);
    RUN_TEST(test_mfa_method_response_parses_an_empty_object);
    RUN_TEST(test_mfa_method_response_rejects_a_non_object);
    RUN_TEST(test_mfa_policy_round_trips);
    RUN_TEST(test_mfa_policy_parses_an_empty_object);
    RUN_TEST(test_mfa_policy_rejects_a_non_object);
    RUN_TEST(test_migrate_custody_response_round_trips);
    RUN_TEST(test_migrate_custody_response_parses_without_optionals);
    RUN_TEST(test_migrate_custody_response_parses_an_empty_object);
    RUN_TEST(test_migrate_custody_response_rejects_a_non_object);
    RUN_TEST(test_mtls_trust_anchor_response_round_trips);
    RUN_TEST(test_mtls_trust_anchor_response_parses_without_optionals);
    RUN_TEST(test_mtls_trust_anchor_response_parses_an_empty_object);
    RUN_TEST(test_mtls_trust_anchor_response_rejects_a_non_object);
    RUN_TEST(test_notification_policy_round_trips);
    RUN_TEST(test_notification_policy_parses_an_empty_object);
    RUN_TEST(test_notification_policy_rejects_a_non_object);
    RUN_TEST(test_notification_rule_response_round_trips);
    RUN_TEST(test_notification_rule_response_parses_an_empty_object);
    RUN_TEST(test_notification_rule_response_rejects_a_non_object);
    RUN_TEST(test_o_auth2_client_created_response_round_trips);
    RUN_TEST(test_o_auth2_client_created_response_parses_an_empty_object);
    RUN_TEST(test_o_auth2_client_created_response_rejects_a_non_object);
    RUN_TEST(test_o_auth2_client_response_round_trips);
    RUN_TEST(test_o_auth2_client_response_parses_without_optionals);
    RUN_TEST(test_o_auth2_client_response_parses_an_empty_object);
    RUN_TEST(test_o_auth2_client_response_rejects_a_non_object);
    RUN_TEST(test_oidc_authorize_request_round_trips);
    RUN_TEST(test_oidc_authorize_request_parses_an_empty_object);
    RUN_TEST(test_oidc_authorize_request_rejects_a_non_object);
    RUN_TEST(test_oidc_authorize_response_round_trips);
    RUN_TEST(test_oidc_authorize_response_parses_an_empty_object);
    RUN_TEST(test_oidc_authorize_response_rejects_a_non_object);
    RUN_TEST(test_oidc_callback_request_round_trips);
    RUN_TEST(test_oidc_callback_request_parses_an_empty_object);
    RUN_TEST(test_oidc_callback_request_rejects_a_non_object);
    RUN_TEST(test_oidc_callback_response_round_trips);
    RUN_TEST(test_oidc_callback_response_parses_an_empty_object);
    RUN_TEST(test_oidc_callback_response_rejects_a_non_object);
    RUN_TEST(test_opaque_enrollment_round_trips);
    RUN_TEST(test_opaque_enrollment_parses_an_empty_object);
    RUN_TEST(test_opaque_enrollment_rejects_a_non_object);
    RUN_TEST(test_opaque_policy_round_trips);
    RUN_TEST(test_opaque_policy_parses_an_empty_object);
    RUN_TEST(test_opaque_policy_rejects_a_non_object);
    RUN_TEST(test_organization_round_trips);
    RUN_TEST(test_organization_parses_an_empty_object);
    RUN_TEST(test_organization_rejects_a_non_object);
    RUN_TEST(test_password_policy_round_trips);
    RUN_TEST(test_password_policy_parses_an_empty_object);
    RUN_TEST(test_password_policy_rejects_a_non_object);
    RUN_TEST(test_permission_round_trips);
    RUN_TEST(test_permission_parses_an_empty_object);
    RUN_TEST(test_permission_rejects_a_non_object);
    RUN_TEST(test_pgp_key_round_trips);
    RUN_TEST(test_pgp_key_parses_an_empty_object);
    RUN_TEST(test_pgp_key_rejects_a_non_object);
    RUN_TEST(test_policy_response_round_trips);
    RUN_TEST(test_policy_response_parses_without_optionals);
    RUN_TEST(test_policy_response_parses_an_empty_object);
    RUN_TEST(test_policy_response_rejects_a_non_object);
    RUN_TEST(test_privacy_policy_round_trips);
    RUN_TEST(test_privacy_policy_parses_an_empty_object);
    RUN_TEST(test_privacy_policy_rejects_a_non_object);
    RUN_TEST(test_reactor_event_descriptor_round_trips);
    RUN_TEST(test_reactor_event_descriptor_parses_an_empty_object);
    RUN_TEST(test_reactor_event_descriptor_rejects_a_non_object);
    RUN_TEST(test_reactor_response_round_trips);
    RUN_TEST(test_reactor_response_parses_without_optionals);
    RUN_TEST(test_reactor_response_parses_an_empty_object);
    RUN_TEST(test_reactor_response_rejects_a_non_object);
    RUN_TEST(test_ready_response_round_trips);
    RUN_TEST(test_ready_response_parses_an_empty_object);
    RUN_TEST(test_ready_response_rejects_a_non_object);
    RUN_TEST(test_resolved_permission_grant_round_trips);
    RUN_TEST(test_resolved_permission_grant_parses_an_empty_object);
    RUN_TEST(test_resolved_permission_grant_rejects_a_non_object);
    RUN_TEST(test_resource_round_trips);
    RUN_TEST(test_resource_parses_without_optionals);
    RUN_TEST(test_resource_parses_an_empty_object);
    RUN_TEST(test_resource_rejects_a_non_object);
    RUN_TEST(test_retry_policy_round_trips);
    RUN_TEST(test_retry_policy_parses_an_empty_object);
    RUN_TEST(test_retry_policy_rejects_a_non_object);
    RUN_TEST(test_role_round_trips);
    RUN_TEST(test_role_parses_an_empty_object);
    RUN_TEST(test_role_rejects_a_non_object);
    RUN_TEST(test_role_assignment_round_trips);
    RUN_TEST(test_role_assignment_parses_without_optionals);
    RUN_TEST(test_role_assignment_parses_an_empty_object);
    RUN_TEST(test_role_assignment_rejects_a_non_object);
    RUN_TEST(test_role_group_assignment_round_trips);
    RUN_TEST(test_role_group_assignment_parses_without_optionals);
    RUN_TEST(test_role_group_assignment_parses_an_empty_object);
    RUN_TEST(test_role_group_assignment_rejects_a_non_object);
    RUN_TEST(test_role_service_account_assignment_round_trips);
    RUN_TEST(test_role_service_account_assignment_parses_without_optionals);
    RUN_TEST(test_role_service_account_assignment_parses_an_empty_object);
    RUN_TEST(test_role_service_account_assignment_rejects_a_non_object);
    RUN_TEST(test_role_user_assignment_round_trips);
    RUN_TEST(test_role_user_assignment_parses_without_optionals);
    RUN_TEST(test_role_user_assignment_parses_an_empty_object);
    RUN_TEST(test_role_user_assignment_rejects_a_non_object);
    RUN_TEST(test_rotate_secret_response_round_trips);
    RUN_TEST(test_rotate_secret_response_parses_an_empty_object);
    RUN_TEST(test_rotate_secret_response_rejects_a_non_object);
    RUN_TEST(test_scim_token_response_round_trips);
    RUN_TEST(test_scim_token_response_parses_without_optionals);
    RUN_TEST(test_scim_token_response_parses_an_empty_object);
    RUN_TEST(test_scim_token_response_rejects_a_non_object);
    RUN_TEST(test_scope_round_trips);
    RUN_TEST(test_scope_parses_an_empty_object);
    RUN_TEST(test_scope_rejects_a_non_object);
    RUN_TEST(test_security_settings_round_trips);
    RUN_TEST(test_security_settings_parses_an_empty_object);
    RUN_TEST(test_security_settings_rejects_a_non_object);
    RUN_TEST(test_service_account_created_response_round_trips);
    RUN_TEST(test_service_account_created_response_parses_without_optionals);
    RUN_TEST(test_service_account_created_response_parses_an_empty_object);
    RUN_TEST(test_service_account_created_response_rejects_a_non_object);
    RUN_TEST(test_service_account_response_round_trips);
    RUN_TEST(test_service_account_response_parses_without_optionals);
    RUN_TEST(test_service_account_response_parses_an_empty_object);
    RUN_TEST(test_service_account_response_rejects_a_non_object);
    RUN_TEST(test_set_mtls_trust_anchor_round_trips);
    RUN_TEST(test_set_mtls_trust_anchor_parses_an_empty_object);
    RUN_TEST(test_set_mtls_trust_anchor_rejects_a_non_object);
    RUN_TEST(test_set_org_email_config_round_trips);
    RUN_TEST(test_set_org_email_config_parses_without_optionals);
    RUN_TEST(test_set_org_email_config_parses_an_empty_object);
    RUN_TEST(test_set_org_email_config_rejects_a_non_object);
    RUN_TEST(test_set_org_settings_round_trips);
    RUN_TEST(test_set_org_settings_parses_without_optionals);
    RUN_TEST(test_set_org_settings_parses_an_empty_object);
    RUN_TEST(test_set_org_settings_rejects_a_non_object);
    RUN_TEST(test_sign_audit_batch_request_round_trips);
    RUN_TEST(test_sign_audit_batch_request_parses_an_empty_object);
    RUN_TEST(test_sign_audit_batch_request_rejects_a_non_object);
    RUN_TEST(test_sign_intermediate_csr_request_round_trips);
    RUN_TEST(test_sign_intermediate_csr_request_parses_an_empty_object);
    RUN_TEST(test_sign_intermediate_csr_request_rejects_a_non_object);
    RUN_TEST(test_signed_audit_batch_round_trips);
    RUN_TEST(test_signed_audit_batch_parses_an_empty_object);
    RUN_TEST(test_signed_audit_batch_rejects_a_non_object);
    RUN_TEST(test_smtp_config_round_trips);
    RUN_TEST(test_smtp_config_parses_an_empty_object);
    RUN_TEST(test_smtp_config_rejects_a_non_object);
    RUN_TEST(test_tenant_round_trips);
    RUN_TEST(test_tenant_parses_without_optionals);
    RUN_TEST(test_tenant_parses_an_empty_object);
    RUN_TEST(test_tenant_rejects_a_non_object);
    RUN_TEST(test_tenant_settings_override_round_trips);
    RUN_TEST(test_tenant_settings_override_parses_without_optionals);
    RUN_TEST(test_tenant_settings_override_parses_an_empty_object);
    RUN_TEST(test_tenant_settings_override_rejects_a_non_object);
    RUN_TEST(test_token_exchange_trust_request_round_trips);
    RUN_TEST(test_token_exchange_trust_request_parses_without_optionals);
    RUN_TEST(test_token_exchange_trust_request_parses_an_empty_object);
    RUN_TEST(test_token_exchange_trust_request_rejects_a_non_object);
    RUN_TEST(test_token_exchange_trust_response_round_trips);
    RUN_TEST(test_token_exchange_trust_response_parses_without_optionals);
    RUN_TEST(test_token_exchange_trust_response_parses_an_empty_object);
    RUN_TEST(test_token_exchange_trust_response_rejects_a_non_object);
    RUN_TEST(test_token_policy_round_trips);
    RUN_TEST(test_token_policy_parses_an_empty_object);
    RUN_TEST(test_token_policy_rejects_a_non_object);
    RUN_TEST(test_update_federation_config_request_round_trips);
    RUN_TEST(test_update_federation_config_request_parses_without_optionals);
    RUN_TEST(test_update_federation_config_request_parses_an_empty_object);
    RUN_TEST(test_update_federation_config_request_rejects_a_non_object);
    RUN_TEST(test_update_group_round_trips);
    RUN_TEST(test_update_group_parses_without_optionals);
    RUN_TEST(test_update_group_parses_an_empty_object);
    RUN_TEST(test_update_group_rejects_a_non_object);
    RUN_TEST(test_update_notification_rule_request_round_trips);
    RUN_TEST(test_update_notification_rule_request_parses_without_optionals);
    RUN_TEST(test_update_notification_rule_request_parses_an_empty_object);
    RUN_TEST(test_update_notification_rule_request_rejects_a_non_object);
    RUN_TEST(test_update_o_auth2_client_request_round_trips);
    RUN_TEST(test_update_o_auth2_client_request_parses_without_optionals);
    RUN_TEST(test_update_o_auth2_client_request_parses_an_empty_object);
    RUN_TEST(test_update_o_auth2_client_request_rejects_a_non_object);
    RUN_TEST(test_update_organization_request_round_trips);
    RUN_TEST(test_update_organization_request_parses_without_optionals);
    RUN_TEST(test_update_organization_request_parses_an_empty_object);
    RUN_TEST(test_update_organization_request_rejects_a_non_object);
    RUN_TEST(test_update_permission_request_round_trips);
    RUN_TEST(test_update_permission_request_parses_without_optionals);
    RUN_TEST(test_update_permission_request_parses_an_empty_object);
    RUN_TEST(test_update_permission_request_rejects_a_non_object);
    RUN_TEST(test_update_reactor_request_round_trips);
    RUN_TEST(test_update_reactor_request_parses_without_optionals);
    RUN_TEST(test_update_reactor_request_parses_an_empty_object);
    RUN_TEST(test_update_reactor_request_rejects_a_non_object);
    RUN_TEST(test_update_resource_request_round_trips);
    RUN_TEST(test_update_resource_request_parses_without_optionals);
    RUN_TEST(test_update_resource_request_parses_an_empty_object);
    RUN_TEST(test_update_resource_request_rejects_a_non_object);
    RUN_TEST(test_update_role_round_trips);
    RUN_TEST(test_update_role_parses_without_optionals);
    RUN_TEST(test_update_role_parses_an_empty_object);
    RUN_TEST(test_update_role_rejects_a_non_object);
    RUN_TEST(test_update_scope_request_round_trips);
    RUN_TEST(test_update_scope_request_parses_without_optionals);
    RUN_TEST(test_update_scope_request_parses_an_empty_object);
    RUN_TEST(test_update_scope_request_rejects_a_non_object);
    RUN_TEST(test_update_service_account_round_trips);
    RUN_TEST(test_update_service_account_parses_without_optionals);
    RUN_TEST(test_update_service_account_parses_an_empty_object);
    RUN_TEST(test_update_service_account_rejects_a_non_object);
    RUN_TEST(test_update_tenant_round_trips);
    RUN_TEST(test_update_tenant_parses_without_optionals);
    RUN_TEST(test_update_tenant_parses_an_empty_object);
    RUN_TEST(test_update_tenant_rejects_a_non_object);
    RUN_TEST(test_update_user_request_round_trips);
    RUN_TEST(test_update_user_request_parses_without_optionals);
    RUN_TEST(test_update_user_request_parses_an_empty_object);
    RUN_TEST(test_update_user_request_rejects_a_non_object);
    RUN_TEST(test_update_webhook_request_round_trips);
    RUN_TEST(test_update_webhook_request_parses_without_optionals);
    RUN_TEST(test_update_webhook_request_parses_an_empty_object);
    RUN_TEST(test_update_webhook_request_rejects_a_non_object);
    RUN_TEST(test_user_response_round_trips);
    RUN_TEST(test_user_response_parses_without_optionals);
    RUN_TEST(test_user_response_parses_an_empty_object);
    RUN_TEST(test_user_response_rejects_a_non_object);
    RUN_TEST(test_webauthn_attestation_policy_round_trips);
    RUN_TEST(test_webauthn_attestation_policy_parses_without_optionals);
    RUN_TEST(test_webauthn_attestation_policy_parses_an_empty_object);
    RUN_TEST(test_webauthn_attestation_policy_rejects_a_non_object);
    RUN_TEST(test_webhook_response_round_trips);
    RUN_TEST(test_webhook_response_parses_an_empty_object);
    RUN_TEST(test_webhook_response_rejects_a_non_object);
    return UNITY_END();
}
